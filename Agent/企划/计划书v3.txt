MfkAgent 开发路线计划书 (精简同步版)基线状态：同步至 v74.2 | 当前主线：Phase 8 (Windows 交付就绪)一、 产品定位与裁剪原则定位调整：从“Agent 平台/Marketplace/插件生态”全面收拢为 “个人 AI 本地工作助手”（用户 ➔ Project ➔ Chat ➔ Agent 辅助 ➔ 完成任务）。废弃/归档内容：用户自定义 Agent、Marketplace、Workflow 引擎、MCP 管理中心。（代码已保留并归档，不进入 V1 核心主线）。二、 核心架构现状 (Status Quarantine)✅ 已完成闭环 (Phase 0 - 6 & 核心演进)基础架构：SQLite 数据层、User -> Workspace -> Project -> Chat -> Message。AI Runtime：单一 AgentRuntime 执行链、12 家 Provider（含 SiliconFlow）、Plan(只读)/Build(可写) 双模式。TaskGraph & 动态自愈：LLM 辅助规划 (Planner)、DAG 任务图推进，以及 任务失败后的反思与动态节点注入 (Self-Healing)。体验与上下文：三段式会话压缩、思考过程裁剪、前端 SSE agent_state_update 状态卡片可视化。⚠️ 已冻结模块 (Phase 7)MCP 支持、插件系统、Workflow 引擎、自动任务（代码保留标记归档，等待后续架构统一）。三、 当前攻坚主线：Phase 8 (Windows 交付就绪 - P0 级)最高优先级：★★★★★ | 目标：打造可直接打包交付的 Windows 桌面软件序号任务模块核心执行动作8.1打包链路修复启用 output: 'export' 并补全 generateStaticParams，恢复 out/ 静态产出。8.2主进程生命周期Electron main.js 实现 spawn uvicorn 启动后端，before-quit 时通过 taskkill 清理子进程树。8.3存储路径解耦重构 config.py，将 CWD 相对路径全面改为基于 Path(__file__) 的绝对路径，清理根目录遗留 DB。8.4工具安全沙箱重构 services/tools.py，为文件读写工具补全路径穿越（Path Traversal）防范，subprocess 显式强制 UTF-8。8.5桌面端体验补全配置国内 Electron 镜像、挂载 .ico 图标、增加单实例锁（Single Instance Lock）。四、 后续演进路线 (Phase 9 - 13)Phase 8 (P0 打包与沙箱)
       │
       ▼
Phase 9 (P1 端口与长路径兼容) ──► Phase 11 (Agent 轮次扩展与自查)
       │                                │
       ▼                                ▼
Phase 10 (Memory 自动提取)    ──► Phase 12/13 (推理模式与首 Token 优化)
1. Phase 9：体验打磨与稳定性 (★★★★)端口占用检测与自动避让避障。os.walk 过滤 Windows junction 软链接，长路径挂载 \\?\ 前缀。导出文件名过滤 Windows 非法字符。2. Phase 10：Memory 自动提取系统 (★★★)目标：从“依赖模型主动调 add_memory 工具”升级为“对话结束后后台自动分析提取”。实现：引入 MemoryExtractor 服务，对对话进行语义分析，过滤临时信息，自动提炼 preference / fact / workflow 并去重入库。3. Phase 11：Agent 能力补全 (★★★★)工具轮次：max_tool_rounds 从 4 扩展至 8-10 可配置。强制自查：任务完成后，强制触发一轮 git diff 或代码测试自审。命令白名单：受控开放 pip / npm / git push，带安全二次确认。4. Phase 12 & 13：模型与推理体验修复 (★★★★)推理模式补全：为各 Provider 映射独立推理档位（解决 none 档无法真正关闭思考的问题）。首 Token 占位：透传 reasoning_content，前端流式期间增加“正在思考…”渲染占位，消除白屏等待感。