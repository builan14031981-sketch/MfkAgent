[pytest]
# 让 async def test_* 自动按 asyncio 运行（pytest-asyncio auto 模式）。
# 背景：test_agent_runtime_phase3 等文件大量使用 async def 测试但未逐点打
# @pytest.mark.asyncio；缺此配置时 pytest 报
# "async def functions are not natively supported"。auto 模式对已显式标记的
# 测试无影响（标记优先），仅把未标记的 async 测试纳入事件循环执行。
asyncio_mode = auto
