# MfkAgent 项目交接文档

**更新日期**: 2026年7月29日  
**更新人**: 前端 AI  
**版本**: v30.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0-5: 全部完成
- ✅ Phase 6: 知识库服务（索引/搜索/接入聊天）
- ✅ 前端 UI 重构（面板系统 + 设置界面）

---

# 二、前端 UI 重构完成内容

## 2.1 AI 回复展示重构

- 移除 AI 回复的气泡样式
- AI 回复改为左侧对齐、全宽显示
- 添加 AI 标识（头像+名称）
- 添加操作按钮（复制/引用/重新生成）- 图标化

## 2.2 面板系统

- 创建 Panel 基础组件（支持 center 和 bottom-left 两种模式）
- 创建 SettingsPanel 组件
- 创建 MemoryPanel 组件

## 2.3 设置界面重构

- 居中浮动面板（700px 宽）
- 左侧分类导航 + 右侧设置内容
- 主题/语言/字体使用胶囊选择器
- 设置项同行布局

## 2.4 全局 Sidebar

- 创建全局 Sidebar 组件
- 聊天页面和主页使用统一 Sidebar

## 2.5 模型选项

- 添加 DeepSeek 模型选项
- 添加 MiMo v2.5 模型选项

---

# 三、前端修改文件

| 文件 | 修改内容 |
|------|----------|
| `components/panels/Panel.tsx` | 面板基础组件 |
| `components/panels/SettingsPanel.tsx` | 设置面板 |
| `components/panels/MemoryPanel.tsx` | 记忆面板 |
| `components/Sidebar.tsx` | 全局 Sidebar |
| `app/page.tsx` | 主页使用面板 |
| `app/chat/[id]/page.tsx` | 聊天页使用面板 |
| `app/globals.css` | 面板动画 |

---

# 四、新增 API

## Knowledge API (`/api/knowledge`)

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/knowledge/index` | 索引项目文件 |
| POST | `/api/knowledge/search` | 搜索知识库 |
| GET | `/api/knowledge/context/{project_id}?query=&max_tokens=` | 获取上下文 |
| GET | `/api/knowledge/stats` | 索引统计 |

### 索引项目

`POST /api/knowledge/index`

```json
{
  "project_id": 1,
  "force": false
}
```

返回：
```json
{
  "project_id": 1,
  "indexed": 86,
  "skipped": 24,
  "errors": 0,
  "total_chunks": 750
}
```

### 搜索

`POST /api/knowledge/search`

```json
{
  "project_id": 1,
  "query": "FastAPI",
  "limit": 5
}
```

返回：
```json
{
  "results": [
    {
      "file_path": "backend/main.py",
      "chunk_index": 0,
      "content": "from fastapi import FastAPI...",
      "score": 3
    }
  ]
}
```

## 聊天集成

当 Chat 关联了 Project 时，`POST /api/chat/{id}/send` 会自动：
1. 搜索项目知识库
2. 将相关文件内容注入 system_prompt
3. AI 回复时可参考项目代码

---

# 五、完整 API 列表（36 个端点）

## Projects (7)
## Chat (10)
## Agents (2)
## Models (5)
## Settings (4)
## Memory (3)
## Backup (2)
## Knowledge (4)

---

# 六、当前前端页面

| 页面 | 路径 | 状态 |
|------|------|------|
| 首页 | `/` | ✅ |
| 聊天页面 | `/chat/[id]` | ✅ |

**已删除页面**（改为面板）：
- `/settings` → SettingsPanel
- `/memory` → MemoryPanel

---

# 七、当前前端组件

| 组件 | 说明 |
|------|------|
| `Panel` | 面板基础组件（center/bottom-left） |
| `SettingsPanel` | 设置面板 |
| `MemoryPanel` | 记忆面板 |
| `Sidebar` | 全局 Sidebar |

---

# 八、已知问题

1. Agent 表中文名在 Windows 终端显示乱码（数据正确，终端编码问题）

---

**文档版本**: v30.0  
**最后更新**: 2026年7月29日
