# MfkAgent 项目交接文档

**更新日期**: 2026年7月28日  
**更新人**: 后端 AI  
**版本**: v25.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0-3: 全部完成
- ✅ Settings / Memory API
- ✅ API 统一错误格式
- ✅ Chat PATCH / 分页
- ✅ 项目文件目录
- ✅ 文件内容读取 🆕
- ✅ 文件搜索 🆕
- ✅ 多模型 Key 配置 🆕

---

# 二、新增 API

## 2.1 文件内容读取

`GET /api/projects/{id}/file?path=backend/main.py`

返回：
```json
{
  "path": "backend/main.py",
  "content": "from fastapi import FastAPI...",
  "size": 1638,
  "encoding": "utf-8"
}
```

限制：
- 最大 100KB
- 不支持二进制文件
- 自动检测编码（UTF-8 / GBK）

## 2.2 文件搜索

`GET /api/projects/{id}/search?q=main&limit=20`

返回：
```json
[
  {
    "name": "main.py",
    "path": "backend/main.py",
    "is_dir": false,
    "size": 1638
  }
]
```

- 按文件名搜索
- 隐藏 `.开头` 文件
- 目录排前面

## 2.3 多模型 Key 配置

API Key 优先从数据库读取，其次从 `.env`。

设置 Key：
```
PUT /api/settings/api_key_deepseek
{ "value": "sk-xxx" }
```

刷新模型配置：
```
POST /api/models/reload
```

支持的 Key：
| 设置 Key | 模型 |
|----------|------|
| `api_key_mimo` | 小米 MiMo |
| `api_key_deepseek` | DeepSeek |
| `api_key_qwen` | 通义千问 |
| `api_key_glm` | 智谱 AI |
| `api_key_moonshot` | Moonshot |

---

# 三、完整 API 列表（28 个端点）

## Projects (7)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/projects?page=&limit=` | 列表 |
| POST | `/api/projects` | 创建 |
| GET | `/api/projects/{id}` | 详情 |
| DELETE | `/api/projects/{id}` | 删除 |
| GET | `/api/projects/{id}/files?subpath=` | 目录 |
| GET | `/api/projects/{id}/file?path=` | 🆕 文件内容 |
| GET | `/api/projects/{id}/search?q=&limit=` | 🆕 搜索 |

## Chat (9)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/chat?page=&limit=&project_id=` | 列表 |
| POST | `/api/chat` | 创建 |
| GET | `/api/chat/{id}` | 详情 |
| PATCH | `/api/chat/{id}` | 修改 |
| DELETE | `/api/chat/{id}` | 删除 |
| GET | `/api/chat/{id}/messages` | 消息 |
| POST | `/api/chat/{id}/messages` | 创建消息 |
| POST | `/api/chat/{id}/send` | 发送+AI |
| POST | `/api/chat/{id}/send/stream` | 流式 |

## Agents (2)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/agents` | 列表 |
| GET | `/api/agents/{id}` | 详情 |

## Models (5)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/models/models` | 模型列表 |
| POST | `/api/models/chat` | 同步聊天 |
| POST | `/api/models/chat/stream` | 流式聊天 |
| GET | `/api/models/providers` | 提供商 |
| POST | `/api/models/reload` | 🆕 重载配置 |

## Settings (4)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/settings` | 所有设置 |
| GET | `/api/settings/{key}` | 单个设置 |
| PUT | `/api/settings/{key}` | 更新 |
| PUT | `/api/settings` | 批量更新 |

## Memory (3)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/memory/{agent_id}` | 列表 |
| POST | `/api/memory` | 创建/更新 |
| DELETE | `/api/memory/{id}` | 删除 |

---

**文档版本**: v25.0  
**最后更新**: 2026年7月28日（后端 AI）
