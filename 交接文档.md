# MfkAgent 项目交接文档

**更新日期**: 2026年7月28日  
**更新人**: 前端 AI  
**版本**: v24.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0-3: 基础框架 + 数据层 + 聊天 + Agent
- ✅ Settings API + 前端页面
- ✅ Memory API + 前端页面
- ✅ API 统一错误格式
- ✅ Chat PATCH 端点 + 前端接入
- ✅ 列表分页 + 前端接入
- ✅ 项目文件目录读取 + hook

## 当前阶段

Phase 1-3 前后端全部完成。可进入 Phase 4。

---

# 二、前端 AI 完成内容（本次）

## 2.1 完成任务

| 任务 | 状态 | 说明 |
|------|------|------|
| 更新 useChat hook | ✅ | 支持分页和 PATCH |
| 更新 useProjects hook | ✅ | 支持分页 |
| 创建 useProjectFiles hook | ✅ | 读取项目文件目录 |
| 聊天页面修改标题 | ✅ | 点击标题可编辑 |

## 2.2 修改文件

- `frontend/src/hooks/useChat.ts` — 更新
- `frontend/src/hooks/useProjects.ts` — 更新
- `frontend/src/hooks/useProjectFiles.ts` — 新建
- `frontend/src/app/chat/[id]/page.tsx` — 更新

## 2.3 后端依赖

- `PATCH /api/chat/{id}` — 修改聊天标题
- `GET /api/projects?page=&limit=` — 分页
- `GET /api/chat?page=&limit=` — 分页
- `GET /api/projects/{id}/files` — 文件目录

---

# 三、当前 API 列表

## Projects

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/projects?page=&limit=` | 列表（分页） |
| POST | `/api/projects` | 创建 |
| GET | `/api/projects/{id}` | 详情 |
| DELETE | `/api/projects/{id}` | 删除 |
| GET | `/api/projects/{id}/files?subpath=` | 文件目录 |

## Chat

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/chat?page=&limit=&project_id=` | 列表（分页） |
| POST | `/api/chat` | 创建 |
| GET | `/api/chat/{id}` | 详情 |
| PATCH | `/api/chat/{id}` | 修改 |
| DELETE | `/api/chat/{id}` | 删除 |
| GET | `/api/chat/{id}/messages` | 消息列表 |
| POST | `/api/chat/{id}/messages` | 创建消息 |
| POST | `/api/chat/{id}/send` | 发送+AI回复 |
| POST | `/api/chat/{id}/send/stream` | 流式发送 |

## Agents

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/agents` | 列表 |
| GET | `/api/agents/{id}` | 详情 |

## Models

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/models/models` | 模型列表 |
| POST | `/api/models/chat` | 同步聊天 |
| POST | `/api/models/chat/stream` | 流式聊天 |
| GET | `/api/models/providers` | 提供商 |

## Settings

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/settings` | 所有设置 |
| GET | `/api/settings/{key}` | 单个设置 |
| PUT | `/api/settings/{key}` | 更新 |
| PUT | `/api/settings` | 批量更新 |

## Memory

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/memory/{agent_id}` | 列表 |
| POST | `/api/memory` | 创建/更新 |
| DELETE | `/api/memory/{id}` | 删除 |

---

# 四、已知问题

1. Agent 表中文名在 Windows 终端显示乱码（数据正确，终端编码问题）
2. `.env` 中大部分 API Key 为空（仅 MiMo 可用）

---

**文档版本**: v24.0  
**最后更新**: 2026年7月28日

---

# 二、新增功能

## 2.1 API 统一错误格式

所有错误返回：
```json
{
  "error": "错误信息",
  "code": 404
}
```

验证错误：
```json
{
  "error": "Validation error",
  "code": 422,
  "details": ["body -> name: field required"]
}
```

## 2.2 Chat PATCH 端点

`PATCH /api/chat/{id}`

修改标题或切换 Agent：
```json
{
  "title": "新标题",
  "agent_id": "coder"
}
```

两个字段都是可选的。

## 2.3 列表分页

`GET /api/projects?page=1&limit=20`

返回：
```json
{
  "items": [...],
  "total": 50,
  "page": 1,
  "limit": 20,
  "pages": 3
}
```

适用于 `/api/projects` 和 `/api/chat`。

## 2.4 项目文件目录

`GET /api/projects/{id}/files?subpath=backend`

返回：
```json
[
  {
    "name": "main.py",
    "path": "backend/main.py",
    "is_dir": false,
    "size": 1234
  },
  {
    "name": "app",
    "path": "backend/app",
    "is_dir": true,
    "size": 0
  }
]
```

- 自动隐藏 `.开头` 的文件/目录
- 目录排前面，文件排后面
- 支持 `subpath` 参数浏览子目录

---

# 三、完整 API 列表

## Projects

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/projects?page=&limit=` | 列表（分页） |
| POST | `/api/projects` | 创建 |
| GET | `/api/projects/{id}` | 详情 |
| DELETE | `/api/projects/{id}` | 删除 |
| GET | `/api/projects/{id}/files?subpath=` | 🆕 文件目录 |

## Chat

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/chat?page=&limit=&project_id=` | 列表（分页） |
| POST | `/api/chat` | 创建 |
| GET | `/api/chat/{id}` | 详情 |
| PATCH | `/api/chat/{id}` | 🆕 修改 |
| DELETE | `/api/chat/{id}` | 删除 |
| GET | `/api/chat/{id}/messages` | 消息列表 |
| POST | `/api/chat/{id}/messages` | 创建消息 |
| POST | `/api/chat/{id}/send` | 发送+AI回复 |
| POST | `/api/chat/{id}/send/stream` | 流式发送 |

## Agents

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/agents` | 列表 |
| GET | `/api/agents/{id}` | 详情 |

## Models

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/models/models` | 模型列表 |
| POST | `/api/models/chat` | 同步聊天 |
| POST | `/api/models/chat/stream` | 流式聊天 |
| GET | `/api/models/providers` | 提供商 |

## Settings

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/settings` | 所有设置 |
| GET | `/api/settings/{key}` | 单个设置 |
| PUT | `/api/settings/{key}` | 更新 |
| PUT | `/api/settings` | 批量更新 |

## Memory

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/memory/{agent_id}` | 列表 |
| POST | `/api/memory` | 创建/更新 |
| DELETE | `/api/memory/{id}` | 删除 |

---

**文档版本**: v23.0  
**最后更新**: 2026年7月28日（后端 AI）
