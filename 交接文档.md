# MfkAgent 项目交接文档

**更新日期**: 2026年7月28日  
**更新人**: 后端 AI  
**版本**: v18.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0: 项目稳定化
- ✅ Phase 1: 基础数据层与API

## 当前阶段

Phase 1 完成，后端 API 可用。

---

# 二、当前 API 列表

## 2.1 Projects API

**前缀**: `/api/projects`

### GET /api/projects

列出所有项目。

**请求**: 无参数

**返回**:
```json
[
  {
    "id": 1,
    "name": "MfkAgent",
    "path": "E:/智慧项目/Mfkagent",
    "created_at": "2026-07-28T12:00:00",
    "updated_at": "2026-07-28T12:00:00"
  }
]
```

### POST /api/projects

创建项目。如果 path 已存在则返回已有项目。

**请求**:
```json
{
  "name": "项目名称",
  "path": "E:/path/to/project"
}
```

**返回**: ProjectResponse 对象

### GET /api/projects/{project_id}

获取单个项目。

**返回**: ProjectResponse 对象

**错误**: 404 如果不存在

### DELETE /api/projects/{project_id}

删除项目（级联删除关联 Chat 和 Message）。

**返回**: `{"status": "deleted"}`

---

## 2.2 Chat API

**前缀**: `/api/chat`

### GET /api/chat

列出聊天。

**参数**:
- `project_id` (可选, int): 过滤指定项目的聊天
- 不传: 返回普通聊天（project_id 为 null）

**返回**:
```json
[
  {
    "id": 1,
    "project_id": null,
    "agent_id": "warm",
    "title": "New Chat",
    "created_at": "2026-07-28T12:00:00",
    "updated_at": "2026-07-28T12:00:00"
  }
]
```

### POST /api/chat

创建聊天。

**请求**:
```json
{
  "project_id": null,
  "agent_id": "warm",
  "title": "New Chat"
}
```

**返回**: ChatResponse 对象

### GET /api/chat/{chat_id}

获取单个聊天。

**返回**: ChatResponse 对象

### DELETE /api/chat/{chat_id}

删除聊天（级联删除 Message）。

**返回**: `{"status": "deleted"}`

### GET /api/chat/{chat_id}/messages

获取聊天消息列表。

**返回**: MessageResponse 数组，按 created_at 升序

```json
[
  {
    "id": 1,
    "chat_id": 1,
    "role": "user",
    "content": "Hello",
    "created_at": "2026-07-28T12:00:00"
  }
]
```

### POST /api/chat/{chat_id}/messages

创建消息。

**请求**:
```json
{
  "role": "user",
  "content": "消息内容"
}
```

**返回**: MessageResponse 对象

---

## 2.3 Agents API

**前缀**: `/api/agents`

### GET /api/agents

列出预设 Agent。

**返回**:
```json
[
  {
    "id": "warm",
    "name": "小暖",
    "description": "情感理解型助手",
    "avatar": "🌸",
    "system_prompt": "你是一名温暖的AI伙伴..."
  },
  {
    "id": "rational",
    "name": "锐",
    "description": "理性决策型助手",
    "avatar": "⚔️",
    "system_prompt": "..."
  },
  {
    "id": "coder",
    "name": "码农",
    "description": "编程开发助手",
    "avatar": "💻",
    "system_prompt": "..."
  },
  {
    "id": "writer",
    "name": "笔神",
    "description": "写作创作助手",
    "avatar": "✍️",
    "system_prompt": "..."
  }
]
```

### GET /api/agents/{agent_id}

获取单个 Agent。

**返回**: AgentInfo 对象

---

## 2.4 Models API

**前缀**: `/api/models`

### GET /api/models/models

获取可用模型列表（需要配置 API Key）。

**返回**:
```json
[
  {
    "id": "mimo-v2.5-pro",
    "name": "mimo-v2.5-pro",
    "provider": "mimo",
    "max_tokens": 4096
  }
]
```

### POST /api/models/chat

同步聊天请求。

**请求**:
```json
{
  "model": "mimo-v2.5-pro",
  "messages": [
    {"role": "user", "content": "Hello"}
  ],
  "temperature": 0.7,
  "max_tokens": 4096,
  "stream": false
}
```

**返回**:
```json
{
  "id": "chatcmpl-xxx",
  "model": "mimo-v2.5-pro",
  "content": "AI回复内容",
  "finish_reason": "stop",
  "usage": {"prompt_tokens": 10, "completion_tokens": 20, "total_tokens": 30}
}
```

### POST /api/models/chat/stream

流式聊天请求。

**请求**: 同 /api/models/chat

**返回**: SSE 流
```
data: {"content": "你"}
data: {"content": "好"}
data: [DONE]
```

**⚠️ 注意**: 后端 `model_service.chat_stream` 方法当前未实现，此接口会报错。

### GET /api/models/providers

获取模型提供商列表。

---

## 2.5 Memory API

**前缀**: `/api/memory`

**状态**: Stub，全部返回 501

---

# 三、前端调用注意事项

## 3.1 CORS

后端已配置 CORS，允许 `localhost:3000` 和 `localhost:3001`。

## 3.2 时间格式

所有时间字段格式: ISO 8601 (`2026-07-28T12:00:00`)

## 3.3 聊天列表过滤

- 获取普通聊天: `GET /api/chat` (不传 project_id)
- 获取项目聊天: `GET /api/chat?project_id=1`

## 3.4 消息顺序

消息按 `created_at` 升序返回，前端无需排序。

## 3.5 Agent ID

预设 Agent 使用字符串 ID: `warm`, `rational`, `coder`, `writer`

---

# 四、Phase 2 需求检查

## 聊天系统后端需求

| 需求 | 当前状态 | 说明 |
|------|----------|------|
| 创建 Chat | ✅ | POST /api/chat |
| 删除 Chat | ✅ | DELETE /api/chat/{id} |
| 聊天列表 | ✅ | GET /api/chat |
| 发送消息 | ✅ | POST /api/chat/{id}/messages |
| 获取消息 | ✅ | GET /api/chat/{id}/messages |
| AI 回复（同步） | ✅ | POST /api/models/chat |
| AI 回复（流式） | ❌ | `chat_stream` 未实现 |
| 自动保存 | ✅ | 消息创建即保存 |

## 缺失项

**`model_service.chat_stream` 方法未实现**。

`/api/models/chat/stream` 端点会调用此方法但会报错。

需要在 `backend/app/services/model.py` 中实现流式调用。

---

# 五、数据模型

```
projects
  id, name, path, created_at, updated_at

chats
  id, project_id (FK nullable), agent_id, title, created_at, updated_at

messages
  id, chat_id (FK), role, content, created_at

agents
  id, agent_id (unique), name, description, avatar, system_prompt, model, temperature

memories
  id, agent_id, user_id, key, value, memory_type, created_at, updated_at

settings
  id, key (unique), value, updated_at
```

---

# 六、已知问题

1. `chat_stream` 未实现 → 流式输出不可用
2. Memory API 全部 stub → 返回 501
3. Agent 表未使用 → 预设 Agent 硬编码在 agents.py
4. Setting 表未使用 → 无 API 端点

---

**文档版本**: v18.0  
**最后更新**: 2026年7月28日
