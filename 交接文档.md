# MfkAgent 项目交接文档

**更新日期**: 2026年7月28日  
**更新人**: 后端 AI  
**版本**: v21.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0: 项目稳定化
- ✅ Phase 1: 基础数据层与API
- ✅ Phase 2 后端: 流式调用 + 统一聊天端点
- ✅ 前端接入真实数据（Agent + Project）

## 当前阶段

Phase 2 后端完成。前端可开始接入聊天功能。

---

# 二、当前 API 列表

## 2.1 Projects API (`/api/projects`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/projects` | 列出所有项目 |
| POST | `/api/projects` | 创建项目（path 去重） |
| GET | `/api/projects/{id}` | 获取项目详情 |
| DELETE | `/api/projects/{id}` | 删除项目（级联） |

## 2.2 Chat API (`/api/chat`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/chat` | 列出聊天（?project_id= 过滤） |
| POST | `/api/chat` | 创建聊天 |
| GET | `/api/chat/{id}` | 获取聊天详情 |
| DELETE | `/api/chat/{id}` | 删除聊天（级联） |
| GET | `/api/chat/{id}/messages` | 获取消息列表 |
| POST | `/api/chat/{id}/messages` | 创建消息 |
| **POST** | **`/api/chat/{id}/send`** | **发送消息+AI回复+保存（同步）** |
| **POST** | **`/api/chat/{id}/send/stream`** | **发送消息+AI回复+保存（流式）** |

## 2.3 Agents API (`/api/agents`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/agents` | 列出预设 Agent（4个） |
| GET | `/api/agents/{id}` | 获取 Agent 详情 |

## 2.4 Models API (`/api/models`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/models/models` | 可用模型列表 |
| POST | `/api/models/chat` | 同步聊天（底层） |
| POST | `/api/models/chat/stream` | 流式聊天（底层） |
| GET | `/api/models/providers` | 提供商列表 |

## 2.5 Memory API (`/api/memory`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/memory/{agent_id}` | 获取记忆（stub） |
| POST | `/api/memory` | 创建记忆（stub） |
| DELETE | `/api/memory/{id}` | 删除记忆（stub） |

---

# 三、新增端点详细说明

## 3.1 POST /api/chat/{chat_id}/send

统一聊天端点：保存用户消息 → 获取历史 → 调用 AI → 保存 AI 回复。

**请求**:
```json
{
  "content": "用户消息",
  "model": "mimo-v2.5-pro",
  "temperature": 0.7,
  "max_tokens": 4096
}
```

**返回**:
```json
{
  "user_message": {
    "id": 1,
    "chat_id": 1,
    "role": "user",
    "content": "用户消息",
    "created_at": "..."
  },
  "ai_message": {
    "id": 2,
    "chat_id": 1,
    "role": "assistant",
    "content": "AI回复",
    "created_at": "..."
  }
}
```

**特性**:
- 自动使用 Agent 的 system_prompt
- 自动保存用户消息和 AI 回复
- 自动更新 chat.updated_at

## 3.2 POST /api/chat/{chat_id}/send/stream

流式版本，返回 SSE 流。

**请求**: 同 `/send`

**返回**:
```
data: {"content": "你"}
data: {"content": "好"}
data: {"finish_reason": "stop"}
data: [DONE]
```

**特性**:
- 流式返回 AI 回复
- 完成后自动保存到数据库
- 错误时返回 `{"error": "..."}`

---

# 四、前端调用指南

## 4.1 创建聊天并发送消息

```typescript
// 1. 创建聊天
const chat = await fetch('/api/chat', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ agent_id: 'warm', title: 'New Chat' })
}).then(r => r.json());

// 2. 发送消息（同步）
const result = await fetch(`/api/chat/${chat.id}/send`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ content: 'Hello' })
}).then(r => r.json());

// result.user_message — 用户消息
// result.ai_message — AI 回复
```

## 4.2 流式输出

```typescript
const response = await fetch(`/api/chat/${chatId}/send/stream`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ content: 'Hello' })
});

const reader = response.body.getReader();
const decoder = new TextDecoder();

while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  
  const text = decoder.decode(value);
  const lines = text.split('\n');
  
  for (const line of lines) {
    if (line.startsWith('data: ')) {
      const data = JSON.parse(line.slice(6));
      if (data.content) {
        // 追加到 UI
      }
      if (data.finish_reason) {
        // 完成
      }
    }
  }
}
```

## 4.3 获取聊天历史

```typescript
const messages = await fetch(`/api/chat/${chatId}/messages`)
  .then(r => r.json());
// 按 created_at 升序，直接渲染
```

---

# 五、后端 AI 完成内容（本次）

## 5.1 完成任务

| 任务 | 状态 | 说明 |
|------|------|------|
| `model_service.chat_stream` | ✅ | 流式调用 MiMo/DeepSeek 等模型 |
| `POST /api/chat/{id}/send` | ✅ | 统一聊天端点（同步） |
| `POST /api/chat/{id}/send/stream` | ✅ | 统一聊天端点（流式） |
| Agent system_prompt 注入 | ✅ | 自动使用预设 Agent 提示词 |

## 5.2 修改文件

- `backend/app/services/model.py` — 新增 `chat_stream` 方法
- `backend/app/api/chat.py` — 新增 `/send` 和 `/send/stream` 端点

## 5.3 对前端影响

**新增可用接口**:
- `POST /api/chat/{id}/send` — 前端可直接调用，无需自行拼装消息
- `POST /api/chat/{id}/send/stream` — 前端可实现实时流式输出

**前端无需**:
- 手动保存用户消息
- 手动保存 AI 回复
- 拼装 system_prompt

---

# 六、已知问题

1. Memory API 全部 stub → 返回 501
2. Agent 表未使用 → 预设 Agent 硬编码在 agents.py
3. Setting 表未使用 → 无 API 端点

---

**文档版本**: v21.0  
**最后更新**: 2026年7月28日（后端 AI）
