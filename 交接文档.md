# MfkAgent 项目交接文档

**更新日期**: 2026年7月29日  
**更新人**: 后端 AI  
**版本**: v28.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0-4: 全部完成
- ✅ Phase 5: 记忆注入 / Token 计数 / 聊天导出 / 数据库备份

## 当前阶段

Phase 1-5 后端全部完成。可进入 Phase 6（高级能力）。

---

# 二、新增 API

## 2.1 Token 计数

`POST /api/chat/{id}/send` 返回中新增 `token_usage`：

```json
{
  "user_message": {...},
  "ai_message": {...},
  "token_usage": {
    "prompt_tokens": 81,
    "completion_tokens": 14,
    "total_tokens": 95
  }
}
```

## 2.2 聊天导出

`GET /api/chat/{id}/export?format=json|markdown`

返回：
```json
{
  "format": "markdown",
  "content": "# Chat Title\n\n**User**: ...\n\n**Assistant**: ...",
  "filename": "Chat Title.md"
}
```

## 2.3 数据库备份

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/backup/backup` | 创建备份 |
| GET | `/api/backup/backups` | 列出备份 |

- 自动保留最近 10 个备份
- 备份路径：`./backups/`

---

# 三、完整 API 列表（32 个端点）

## Projects (7)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/projects?page=&limit=` | 列表 |
| POST | `/api/projects` | 创建 |
| GET | `/api/projects/{id}` | 详情 |
| DELETE | `/api/projects/{id}` | 删除 |
| GET | `/api/projects/{id}/files?subpath=` | 目录 |
| GET | `/api/projects/{id}/file?path=` | 文件内容 |
| GET | `/api/projects/{id}/search?q=&limit=` | 搜索 |

## Chat (10)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/chat?page=&limit=&project_id=` | 列表 |
| POST | `/api/chat` | 创建 |
| GET | `/api/chat/{id}` | 详情 |
| PATCH | `/api/chat/{id}` | 修改 |
| DELETE | `/api/chat/{id}` | 删除 |
| GET | `/api/chat/{id}/messages` | 消息 |
| POST | `/api/chat/{id}/messages` | 创建消息 |
| POST | `/api/chat/{id}/send` | 发送+AI |
| POST | `/api/chat/{id}/send/stream` | 流式 |
| GET | `/api/chat/{id}/export?format=` | 🆕 导出 |

## Agents (2)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/agents` | 列表 |
| GET | `/api/agents/{id}` | 详情 |

## Models (5)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/models/models` | 模型列表 |
| POST | `/api/models/chat` | 同步聊天 |
| POST | `/api/models/chat/stream` | 流式聊天 |
| GET | `/api/models/providers` | 提供商 |
| POST | `/api/models/reload` | 重载配置 |

## Settings (4)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/settings` | 所有设置 |
| GET | `/api/settings/{key}` | 单个设置 |
| PUT | `/api/settings/{key}` | 更新 |
| PUT | `/api/settings` | 批量更新 |

## Memory (3)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/memory/{agent_id}` | 列表 |
| POST | `/api/memory` | 创建/更新 |
| DELETE | `/api/memory/{id}` | 删除 |

## Backup (2) 🆕

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/backup/backup` | 创建备份 |
| GET | `/api/backup/backups` | 列出备份 |

---

**文档版本**: v28.0  
**最后更新**: 2026年7月29日（后端 AI）
