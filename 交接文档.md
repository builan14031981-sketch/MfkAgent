# MfkAgent 项目交接文档

**更新日期**: 2026年7月28日  
**更新人**: Anxer + AI  
**版本**: v17.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0: 项目稳定化（环境检查、损坏代码清理、Git 初始化）
- ✅ Phase 1: 数据层（Project / Chat / Message 模型、CRUD API）

## 当前阶段

Phase 1 完成，准备进入 Phase 2（聊天系统）。

---

# 二、Phase 0 完成内容

## 环境检查

- Python 3.14.6 ✅
- Node.js v24.18.0 ✅
- 后端 port 8001 运行正常 ✅
- 前端 port 3000 运行正常 ✅

## 损坏代码清理

16 个空字节文件已移至 `_trash/`：

### 后端

- `.env.example`
- `api/agents.py`, `api/chat.py`, `api/memory.py`
- `services/knowledge.py`, `services/memory.py`
- `services/tools/*` (5个文件)

### 前端

- `README.md`, `electron/main.js`, `electron/preload.js`
- `public/globe.svg`, `public/next.svg`

## 重建的文件

所有损坏文件已用符合当前架构的 stub 代码重建。

## Git 初始化

- `.gitignore` 已创建
- 用户: Anxer, 邮箱: 3220389580@qq.com
- 首次提交: `Phase 0: Project stabilization`

---

# 三、Phase 1 完成内容

## 数据模型

文件: `backend/app/models/agent.py`

```python
Project (projects)
  - id, name, path, created_at, updated_at
  - 关系: chats

Chat (chats)
  - id, project_id (nullable), agent_id, title, created_at, updated_at
  - 关系: project, messages

Message (messages)
  - id, chat_id, role, content, created_at
  - 关系: chat

Agent (agents)
  - id, agent_id, name, description, avatar, system_prompt, model, temperature
  - created_at, updated_at

Memory (memories)
  - id, agent_id, user_id, key, value, memory_type
  - created_at, updated_at

Setting (settings)
  - id, key, value, updated_at
```

## API 端点

### Projects (`/api/projects`)

- `GET /` — 列出所有项目
- `POST /` — 创建项目
- `GET /{id}` — 获取项目详情
- `DELETE /{id}` — 删除项目

### Chat (`/api/chat`)

- `GET /` — 列出聊天（可按 project_id 过滤）
- `POST /` — 创建聊天
- `GET /{id}` — 获取聊天详情
- `DELETE /{id}` — 删除聊天
- `GET /{id}/messages` — 获取消息列表
- `POST /{id}/messages` — 创建消息

### Agents (`/api/agents`)

- `GET /` — 列出预设 Agent（4个）
- `GET /{id}` — 获取 Agent 详情

### Models (`/api/models`)

- `GET /models` — 列出可用模型
- `POST /chat` — 聊天请求
- `POST /chat/stream` — 流式聊天
- `GET /providers` — 列出提供商

## 测试验证

所有 CRUD 操作已验证通过：
- Project: Create ✅ List ✅ Delete ✅
- Chat: Create ✅ List ✅ Delete ✅
- Message: Create ✅ List ✅

---

# 四、项目结构

```
MfkAgent/
├── .gitignore
├── 企划书.md
├── 计划书.txt
├── 交接文档.md
├── _trash/                    # 损坏文件备份
├── backend/
│   ├── main.py                # FastAPI 入口
│   ├── .env                   # 环境变量（含 MiMo API Key）
│   ├── requirements.txt
│   └── app/
│       ├── api/
│       │   ├── agents.py      # 预设 Agent API
│       │   ├── chat.py        # Chat + Message CRUD
│       │   ├── memory.py      # Memory stub
│       │   ├── models.py      # 模型调用 API
│       │   └── projects.py    # Project CRUD
│       ├── core/
│       │   ├── config.py      # 配置系统
│       │   └── database.py    # SQLAlchemy 连接
│       ├── models/
│       │   └── agent.py       # 数据模型（6个表）
│       └── services/
│           ├── model.py       # 多模型调用服务
│           ├── memory.py      # Memory stub
│           ├── knowledge.py   # Knowledge stub
│           └── tools/         # Tools stub
├── frontend/
│   ├── package.json
│   ├── electron/
│   │   ├── main.js            # Electron 主进程
│   │   └── preload.js         # Electron 预加载
│   └── src/
│       ├── app/
│       │   ├── globals.css    # 主题系统
│       │   ├── layout.tsx     # 根布局
│       │   └── page.tsx       # 首页
│       ├── components/
│       │   └── providers.tsx  # 主题提供者
│       └── lib/
│           └── store.ts       # Zustand 状态
└── scripts/                   # 测试脚本
```

---

# 五、启动方式

```bash
# 后端
cd backend
python -m uvicorn main:app --host 127.0.0.1 --port 8001

# 前端
cd frontend
npm run dev
```

---

# 六、下一步：Phase 2

聊天系统：
- 对话页面 (`chat/[id]/page.tsx`)
- 消息发送 + AI 回复
- 流式输出
- 聊天历史管理
- 消息保存

---

**文档版本**: v17.0  
**最后更新**: 2026年7月28日
