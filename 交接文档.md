# MfkAgent 项目交接文档

**更新日期**: 2026年7月29日  
**更新人**: 后端 AI（天梁归档整理）  
**版本**: v40.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0-6: 全部完成
- ✅ Phase 7: 高级能力（MCP / Workflow / 自动任务 / 插件系统）— **提前实现**

## ⚠️ 产品方向对齐说明

Phase 7 四个模块属于**计划书标记为"V1 以后"的提前实现**，与《产品企划书 V3.0》第三章产品边界存在以下冲突：

| 企划书约束 | Phase 7 实现 | 处理方式 |
|-----------|-------------|---------|
| ❌ 插件生态 / MCP 管理中心 — "暂不做" | Plugin 系统 + MCP 端点已实现 | 保留代码，归档标注 |
| ❌ Workflow Builder — "暂不做" | Workflow 引擎已实现 | 保留代码，归档标注 |
| ❌ 自动任务编排 — "暂不做" | AutoTask 引擎已实现 | 保留代码，归档标注 |

**当前决策**：代码保留不删除，标记为提前实现能力，待产品方向确认后决定去留。

---

# 二、Phase 7 完成内容（已归档）

## 2.1 MCP 支持

| 属性 | 值 |
|------|-----|
| **分类** | 核心基础设施 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | web_search 工具、execute_code 工具、projects 资源、code_review 提示词 |

**判断依据**：MCP（Model Context Protocol）是 AI-工具通信协议标准。`MCPServer` 实现了协议规范（initialize / resources/list / tools/list / tools/call / prompts/list / prompts/get），属于基础设施层。其预注册的具体工具属于用户能力，但 MCP 服务框架本身是协议中间件。

| 端点 | 说明 |
|------|------|
| `POST /api/mcp` | MCP 协议处理 |
| `GET /api/mcp/capabilities` | 获取能力 |
| `GET /api/mcp/tools` | 工具列表 |
| `GET /api/mcp/resources` | 资源列表 |
| `GET /api/mcp/prompts` | 提示词列表 |

---

## 2.2 插件系统

| 属性 | 值 |
|------|-----|
| **分类** | 用户功能 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | Web Search（ACTIVE）、Code Execution（ACTIVE）、File Operations（ACTIVE） |

**判断依据**：`PluginManager` 提供插件注册/激活/停用/删除/配置的完整生命周期管理，面向用户扩展能力。属于用户功能层，非基础设施。

| 端点 | 说明 |
|------|------|
| `GET /api/plugins` | 插件列表 |
| `POST /api/plugins` | 注册插件 |
| `POST /api/plugins/{id}/activate` | 激活 |
| `POST /api/plugins/{id}/deactivate` | 停用 |
| `PUT /api/plugins/{id}/config` | 更新配置 |
| `DELETE /api/plugins/{id}` | 删除 |

---

## 2.3 Workflow 引擎

| 属性 | 值 |
|------|-----|
| **分类** | 用户功能 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | "code_review" 示例工作流（read_file → analyze 两步） |

**判断依据**：`WorkflowEngine` 支持用户创建多步骤工作流（TOOL / PROMPT / CONDITION / LOOP 四种步骤类型），步骤间通过 `next_step` 链式执行并传递上下文。属于用户功能层的 Workflow Builder。

| 端点 | 说明 |
|------|------|
| `GET /api/workflows` | 工作流列表 |
| `POST /api/workflows` | 创建工作流 |
| `GET /api/workflows/{id}` | 获取详情 |
| `POST /api/workflows/{id}/execute` | 执行工作流 |

---

## 2.4 自动任务

| 属性 | 值 |
|------|-----|
| **分类** | 用户功能 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | "daily_code_review" 定时任务（SCHEDULED，cron: `0 9 * * *`） |

**判断依据**：`AutoTaskEngine` 将 Workflow 包装为可调度任务，支持 MANUAL / SCHEDULED / EVENT 三种触发方式，有完整的生命周期状态机（PENDING → RUNNING → COMPLETED / FAILED / CANCELLED）。属于用户功能层的自动任务编排。

| 端点 | 说明 |
|------|------|
| `GET /api/autotasks` | 任务列表 |
| `POST /api/autotasks` | 创建任务 |
| `POST /api/autotasks/{id}/execute` | 执行任务 |
| `POST /api/autotasks/{id}/cancel` | 取消任务 |
| `DELETE /api/autotasks/{id}` | 删除 |

---

# 三、Phase 7 模块间依赖关系

```
AutoTask ──依赖──▶ Workflow ──依赖──▶ Tool (tool_registry)
                                        │
                    MCP ──独立──▶ 自有工具注册（与 Tool 系统部分重复）
                    Plugin ──独立──▶ 自有插件注册（与 Tool 系统部分重复）
```

**注意**：MCP 和 Plugin 各自预注册了 web_search、code_execution 等工具，与 `app/services/tools.py` 中的 `ToolRegistry` 存在功能重叠。三套工具注册体系（Tool / MCP / Plugin）目前互不通信，属于架构冗余，后续需统一。

---

# 四、完整 API 列表（45 个端点）

| 模块 | 端点数 |
|------|--------|
| Projects | 7 |
| Chat | 10 |
| Agents | 2 |
| Models | 5 |
| Settings | 4 |
| Memory | 3 |
| Backup | 2 |
| Knowledge | 4 |
| Fonts | 2 |
| Tools | 3 |
| MCP | 5 |
| Workflows | 4 |
| AutoTasks | 5 |
| Plugins | 7 |

---

# 五、后端完成总结

| 阶段 | 状态 | 分类 | 备注 |
|------|------|------|------|
| Phase 0-6 | ✅ 完成 | — | V1 核心闭环 |
| Phase 7 MCP | ✅ 完成（提前实现） | 核心基础设施 | 协议框架，无条件启用 |
| Phase 7 Plugin | ✅ 完成（提前实现） | 用户功能 | 与企划书冲突，待确认 |
| Phase 7 Workflow | ✅ 完成（提前实现） | 用户功能 | 与企划书冲突，待确认 |
| Phase 7 AutoTask | ✅ 完成（提前实现） | 用户功能 | 与企划书冲突，待确认 |

---


---

---

# 六、当前 Agent 架构（v40）

## Agent 模型

| 字段 | 类型 | 说明 |
|------|------|------|
| agent_id | String | 唯一标识：general/analyst/coder/writer |
| name | String | 显示名：通用助手/分析师/码农/笔神 |
| identity | Text | 纯工作角色 Prompt（不含 Personality） |
| capabilities | JSON | 工具白名单：[]=无工具，[web_search,...]=允许列表 |
| model | String | 默认推荐模型 |
| system_prompt | Text | 旧字段保留兼容，取值回退到 identity |

## 预设 Agent 能力分配

| agent_id | name | capabilities |
|----------|------|-------------|
| general | 通用助手 | web_search |
| analyst | 分析师 | web_search, fetch_url, read_file |
| coder | 码农 | web_search, read_file, write_file, list_directory, execute_code, format_json |
| writer | 笔神 | web_search, read_file |
| warm | 小暖(旧) | [] (无工具) |
| rational | 锐(旧) | [] (无工具) |

## Chat 模型

| 字段 | 说明 |
|------|------|
| agent_id | 引用 Agent |
| personality_level | 0-100，对话创建时快照 |
| model | 本次对话使用的模型快照 |

## Prompt 组合流程



## Tools 白名单过滤



# 七、Wave 1-4 提交记录

| Wave | 内容 | commit |
|------|------|--------|
| W1 | Agent 加 identity/capabilities，Chat 加 personality_level/model | 07ece91 |
| W2 | 种子数据切换到 general/analyst/coder/writer，API 对齐 | 1ff9412 |
| W3 | Personality 独立模块，identity 替代 system_prompt | 6242b4b |
| W3b | capabilities 白名单过滤 tools | 7d511cb |
| W4a | SendRequest model/personality 从 Chat 快照取默认值 | be50336 |
| W4b | 前端类型同步 + 交接文档 v40 | 本 commit |

**架构状态**：
- Agent/Pers 完全解耦，Agent=工作角色，Personality=Chat级行为参数
- capabilities 白名单生效：空caps=无工具，旧agent=None兼容
- 前端 Agent 接口已同步 capabilities/identity/default_model

**文档版本**: v40.0  
**最后更新**: 2026年7月30日（后端 AI · 天梁归档整理）


# 八、V1 收尾修复

| 修复 | commit | 说明 |
|------|--------|------|
| 默认模型回退 | 28f5e5b | send_message 硬编码 mimo-v2.5-pro → 读取 Settings.default_model |
| 主页模型预选 | db645f4 | 主页 models[0]→Mimo → 读取 Settings.default_model 预选 |
| 模型注册精简 | a083887 | deepseek-chat → deepseek-v4-flash + deepseek-v4-pro |

**当前模型列表**：mimo-v2.5-pro / mimo-v2.5 / deepseek-v4-flash / deepseek-v4-pro
**默认模型链路**：主页预选 → Chat 创建（快照）→ send_message 回退 → Settings.default_model → hard-fallback mimo-v2.5-pro
