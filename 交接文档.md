# MfkAgent 项目交接文档

**更新日期**: 2026年7月28日  
**更新人**: 后端 AI  
**版本**: v22.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0: 项目稳定化
- ✅ Phase 1: 基础数据层与API
- ✅ Phase 2: 流式调用 + 统一聊天端点
- ✅ Phase 3: Agent 系统 + 人格系统
- ✅ Settings API
- ✅ Memory API
- ✅ Chat 标题自动生成

## 当前阶段

后端 Phase 1-3 全部完成。可进入 Phase 4（项目文件感知）。

---

# 二、当前 API 列表

## Projects (`/api/projects`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/projects` | 列出项目 |
| POST | `/api/projects` | 创建项目 |
| GET | `/api/projects/{id}` | 获取详情 |
| DELETE | `/api/projects/{id}` | 删除项目 |

## Chat (`/api/chat`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/chat` | 列出聊天 |
| POST | `/api/chat` | 创建聊天 |
| GET | `/api/chat/{id}` | 获取详情 |
| DELETE | `/api/chat/{id}` | 删除聊天 |
| GET | `/api/chat/{id}/messages` | 消息列表 |
| POST | `/api/chat/{id}/messages` | 创建消息 |
| POST | `/api/chat/{id}/send` | 发送+AI回复（同步） |
| POST | `/api/chat/{id}/send/stream` | 发送+AI回复（流式） |

## Agents (`/api/agents`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/agents` | 预设 Agent 列表 |
| GET | `/api/agents/{id}` | Agent 详情 |

## Models (`/api/models`)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/models/models` | 可用模型 |
| POST | `/api/models/chat` | 同步聊天 |
| POST | `/api/models/chat/stream` | 流式聊天 |
| GET | `/api/models/providers` | 提供商 |

## Settings (`/api/settings`) 🆕

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/settings` | 获取所有设置 |
| GET | `/api/settings/{key}` | 获取单个设置 |
| PUT | `/api/settings/{key}` | 更新设置 |
| PUT | `/api/settings` | 批量更新 |

**默认设置**:
```json
{
  "theme": "system",
  "language": "zh-CN",
  "default_model": "mimo-v2.5-pro",
  "default_personality": "50",
  "font_size": "14"
}
```

## Memory (`/api/memory`) 🆕

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/memory/{agent_id}` | 获取记忆 |
| POST | `/api/memory` | 创建/更新记忆 |
| DELETE | `/api/memory/{id}` | 删除记忆 |

---

# 三、新增功能说明

## 3.1 Chat 标题自动生成

首次发送消息时，自动截取用户消息前 50 字符作为标题。

## 3.2 人格系统

`POST /api/chat/{id}/send` 新增参数：

```json
{
  "content": "...",
  "personality_level": 50
}
```

| 值 | 效果 |
|----|------|
| 0 | 极度感性（共情、陪伴） |
| 25 | 偏感性 |
| 50 | 平衡（默认） |
| 75 | 偏理性 |
| 100 | 极度理性（挑战、审查） |

## 3.3 Agent 从数据库读取

Agent 不再硬编码。通过 `seed_agents.py` 初始化 4 个预设 Agent。

---

# 四、前端调用示例

## 获取设置

```typescript
const settings = await fetch('http://127.0.0.1:8001/api/settings')
  .then(r => r.json());
// { theme: "system", language: "zh-CN", ... }
```

## 更新设置

```typescript
await fetch('http://127.0.0.1:8001/api/settings/theme', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ value: 'dark' })
});
```

## 发送消息（带人格）

```typescript
const result = await fetch(`/api/chat/${chatId}/send`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    content: '我想辞职创业',
    personality_level: 100  // 极度理性审查
  })
}).then(r => r.json());
```

---

# 五、已知问题

1. Agent 表中文名在 Windows 终端显示乱码（数据正确，终端编码问题）
2. `.env` 中大部分 API Key 为空（仅 MiMo 可用）

---

**文档版本**: v22.0  
**最后更新**: 2026年7月28日（后端 AI）
