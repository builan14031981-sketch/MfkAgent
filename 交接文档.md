# MfkAgent 项目交接文档

**更新日期**: 2026年8月1日  
**更新人**: 后端 AI（归档整理）  
**版本**: v49.0

---

# 一、当前项目状态

## 已完成

- ✅ Phase 0-6: 全部完成
- ✅ Phase 7: 高级能力（MCP / Workflow / 自动任务 / 插件系统）— **提前实现**

## ⚠️ 产品方向对齐说明

Phase 7 四个模块属于**计划书标记为"V1 以后"的提前实现**，与《产品企划书 V3.0》第三章产品边界存在以下冲突：

| 企划书约束 | Phase 7 实现 | 处理方式 |
|-----------|-------------|---------|
| ❌ 插件生态 / MCP 管理中心 — "暂不做" | Plugin 系统 + MCP 端点已实现 | 保留代码，归档标注 |
| ❌ Workflow Builder — "暂不做" | Workflow 引擎已实现 | 保留代码，归档标注 |
| ❌ 自动任务编排 — "暂不做" | AutoTask 引擎已实现 | 保留代码，归档标注 |

**当前决策**：代码保留不删除，标记为提前实现能力，待产品方向确认后决定去留。

---

# 二、Phase 7 完成内容（已归档）

## 2.1 MCP 支持

| 属性 | 值 |
|------|-----|
| **分类** | 核心基础设施 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | web_search 工具、execute_code 工具、projects 资源、code_review 提示词 |

**判断依据**：MCP（Model Context Protocol）是 AI-工具通信协议标准。`MCPServer` 实现了协议规范（initialize / resources/list / tools/list / tools/call / prompts/list / prompts/get），属于基础设施层。其预注册的具体工具属于用户能力，但 MCP 服务框架本身是协议中间件。

| 端点 | 说明 |
|------|------|
| `POST /api/mcp` | MCP 协议处理 |
| `GET /api/mcp/capabilities` | 获取能力 |
| `GET /api/mcp/tools` | 工具列表 |
| `GET /api/mcp/resources` | 资源列表 |
| `GET /api/mcp/prompts` | 提示词列表 |

---

## 2.2 插件系统

| 属性 | 值 |
|------|-----|
| **分类** | 用户功能 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | Web Search（ACTIVE）、Code Execution（ACTIVE）、File Operations（ACTIVE） |

**判断依据**：`PluginManager` 提供插件注册/激活/停用/删除/配置的完整生命周期管理，面向用户扩展能力。属于用户功能层，非基础设施。

| 端点 | 说明 |
|------|------|
| `GET /api/plugins` | 插件列表 |
| `POST /api/plugins` | 注册插件 |
| `POST /api/plugins/{id}/activate` | 激活 |
| `POST /api/plugins/{id}/deactivate` | 停用 |
| `PUT /api/plugins/{id}/config` | 更新配置 |
| `DELETE /api/plugins/{id}` | 删除 |

---

## 2.3 Workflow 引擎

| 属性 | 值 |
|------|-----|
| **分类** | 用户功能 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | "code_review" 示例工作流（read_file → analyze 两步） |

**判断依据**：`WorkflowEngine` 支持用户创建多步骤工作流（TOOL / PROMPT / CONDITION / LOOP 四种步骤类型），步骤间通过 `next_step` 链式执行并传递上下文。属于用户功能层的 Workflow Builder。

| 端点 | 说明 |
|------|------|
| `GET /api/workflows` | 工作流列表 |
| `POST /api/workflows` | 创建工作流 |
| `GET /api/workflows/{id}` | 获取详情 |
| `POST /api/workflows/{id}/execute` | 执行工作流 |

---

## 2.4 自动任务

| 属性 | 值 |
|------|-----|
| **分类** | 用户功能 |
| **实现状态** | 提前实现 |
| **当前启用** | ✅ 是（无条件注册，无开关） |
| **存储方式** | 纯内存（无数据库持久化） |
| **预注册内容** | "daily_code_review" 定时任务（SCHEDULED，cron: `0 9 * * *`） |

**判断依据**：`AutoTaskEngine` 将 Workflow 包装为可调度任务，支持 MANUAL / SCHEDULED / EVENT 三种触发方式，有完整的生命周期状态机（PENDING → RUNNING → COMPLETED / FAILED / CANCELLED）。属于用户功能层的自动任务编排。

| 端点 | 说明 |
|------|------|
| `GET /api/autotasks` | 任务列表 |
| `POST /api/autotasks` | 创建任务 |
| `POST /api/autotasks/{id}/execute` | 执行任务 |
| `POST /api/autotasks/{id}/cancel` | 取消任务 |
| `DELETE /api/autotasks/{id}` | 删除 |

---

# 三、Phase 7 模块间依赖关系

```
AutoTask ──依赖──▶ Workflow ──依赖──▶ Tool (tool_registry)
                                        │
                    MCP ──独立──▶ 自有工具注册（与 Tool 系统部分重复）
                    Plugin ──独立──▶ 自有插件注册（与 Tool 系统部分重复）
```

**注意**：MCP 和 Plugin 各自预注册了 web_search、code_execution 等工具，与 `app/services/tools.py` 中的 `ToolRegistry` 存在功能重叠。三套工具注册体系（Tool / MCP / Plugin）目前互不通信，属于架构冗余，后续需统一。

---

# 四、完整 API 列表（45 个端点）

| 模块 | 端点数 |
|------|--------|
| Projects | 7 |
| Chat | 10 |
| Agents | 2 |
| Models | 5 |
| Settings | 4 |
| Memory | 3 |
| Backup | 2 |
| Knowledge | 4 |
| Fonts | 2 |
| Tools | 3 |
| MCP | 5 |
| Workflows | 4 |
| AutoTasks | 5 |
| Plugins | 7 |

---

# 五、后端完成总结

| 阶段 | 状态 | 分类 | 备注 |
|------|------|------|------|
| Phase 0-6 | ✅ 完成 | — | V1 核心闭环 |
| Phase 7 MCP | ✅ 完成（提前实现） | 核心基础设施 | 协议框架，无条件启用 |
| Phase 7 Plugin | ✅ 完成（提前实现） | 用户功能 | 与企划书冲突，待确认 |
| Phase 7 Workflow | ✅ 完成（提前实现） | 用户功能 | 与企划书冲突，待确认 |
| Phase 7 AutoTask | ✅ 完成（提前实现） | 用户功能 | 与企划书冲突，待确认 |

---


---

---

# 六、当前 Agent 架构（v40）

## Agent 模型

| 字段 | 类型 | 说明 |
|------|------|------|
| agent_id | String | 唯一标识：general/analyst/coder/writer |
| name | String | 显示名：通用助手/分析师/码农/笔神 |
| identity | Text | 纯工作角色 Prompt（不含 Personality） |
| capabilities | JSON | 工具白名单：[]=无工具，[web_search,...]=允许列表 |
| model | String | 默认推荐模型 |
| system_prompt | Text | 旧字段保留兼容，取值回退到 identity |

## 预设 Agent 能力分配

| agent_id | name | capabilities |
|----------|------|-------------|
| general | 通用助手 | web_search |
| analyst | 分析师 | web_search, fetch_url, read_file |
| coder | 码农 | web_search, read_file, write_file, list_directory, execute_code, format_json |
| writer | 笔神 | web_search, read_file |
| warm | 小暖(旧) | [] (无工具) |
| rational | 锐(旧) | [] (无工具) |

## Chat 模型

| 字段 | 说明 |
|------|------|
| agent_id | 引用 Agent |
| personality_level | 0-100，对话创建时快照 |
| model | 本次对话使用的模型快照 |

## Prompt 组合流程



## Tools 白名单过滤



# 七、Wave 1-4 提交记录

| Wave | 内容 | commit |
|------|------|--------|
| W1 | Agent 加 identity/capabilities，Chat 加 personality_level/model | 07ece91 |
| W2 | 种子数据切换到 general/analyst/coder/writer，API 对齐 | 1ff9412 |
| W3 | Personality 独立模块，identity 替代 system_prompt | 6242b4b |
| W3b | capabilities 白名单过滤 tools | 7d511cb |
| W4a | SendRequest model/personality 从 Chat 快照取默认值 | be50336 |
| W4b | 前端类型同步 + 交接文档 v40 | 本 commit |

**架构状态**：
- Agent/Pers 完全解耦，Agent=工作角色，Personality=Chat级行为参数
- capabilities 白名单生效：空caps=无工具，旧agent=None兼容
- 前端 Agent 接口已同步 capabilities/identity/default_model

**文档版本**: v40.0  
**最后更新**: 2026年7月30日（后端 AI · 天梁归档整理）


# 八、V1 收尾修复

| 修复 | commit | 说明 |
|------|--------|------|
| 默认模型回退 | 28f5e5b | send_message 硬编码 mimo-v2.5-pro → 读取 Settings.default_model |
| 主页模型预选 | db645f4 | 主页 models[0]→Mimo → 读取 Settings.default_model 预选 |
| 模型注册精简 | a083887 | deepseek-chat → deepseek-v4-flash + deepseek-v4-pro |

**当前模型列表**：mimo-v2.5-pro / mimo-v2.5 / deepseek-v4-flash / deepseek-v4-pro
**默认模型链路**：主页预选 → Chat 创建（快照）→ send_message 回退 → Settings.default_model → hard-fallback mimo-v2.5-pro

---

# 九、V41 修复：流式接口空值崩溃 + 前端工具面板对齐

**更新日期**: 2026年7月31日  
**版本**: v41.0

## 9.1 后端修复（backend/app/api/chat.py）

`POST /api/chat/{id}/send/stream` 流式发送接口存在两个 bug：

| 修复 | 位置 | 说明 |
|------|------|------|
| ① personality_level 空值崩溃 | `send_message_stream` | 原代码直接 `get_personality_prompt(request.personality_level)`，前端不传该字段时（`None`）触发 `min(100, None)` TypeError → 500。已对齐非流式接口（`send_message`）兜底链：`request.personality_level` → `chat.personality_level`（默认 50） |
| ② DetachedInstanceError | `generate()` 闭包 | 闭包在 `db.close()` 后才访问 detached 的 `chat.model`，抛 "not bound to a Session" → 流式必然失败。已改为在关闭 DB 前提取 `effective_model` / `temperature` / `max_tokens` 局部变量 |

**验证结果**（TestClient + mock 流式）：
- 不传 `personality_level` → ✅ 200，正常 SSE 输出 + `[DONE]`
- 传 `personality_level=100` → ✅ 200，正常流
- assistant 消息落库 → ✅ user + assistant 两条正确写入

## 9.2 前端修复（frontend/src/components/panels/ToolsPanel.tsx）

- `GET /api/tools/definitions` 响应改为读取 `data.definitions`（对齐后端返回结构）
- `POST /api/tools/call` 请求体对齐后端参数：`tool_name` + `arguments`；响应改为展示 `output` 字符串、失败展示 `error`

**文档版本**: v41.0  
**最后更新**: 2026年7月31日（后端 AI · 天梁归档整理）

---

# 十、V42 修复：DeepSeek 模型名映射 + 默认 Agent 更名

**更新日期**: 2026年7月31日  
**版本**: v42.0

## 10.1 DeepSeek 模型名映射（backend/app/services/model.py）

内部展示 ID 与 DeepSeek 官方 API 模型名不一致的问题：内部 ID `deepseek-v4-flash/pro` 原样发往 `api.deepseek.com`，而官方只认 `deepseek-chat` / `deepseek-reasoner`，真实调用会报模型不存在。

新增映射与发送时转换：

| 内部展示 ID | 官方 API 模型名 |
|-------------|----------------|
| `deepseek-v4-flash` | `deepseek-chat` |
| `deepseek-v4-pro` | `deepseek-reasoner` |

- 映射表：`DEEPSEEK_MODEL_MAPPING`（services/model.py）
- 转换方法：`_resolve_api_model_name()` —— 仅 DeepSeek 走映射，其他提供商（Mimo/Qwen/GLM/Moonshot）原样返回
- 生效位置：非流式 `_chat_openai_compatible` 与流式 `chat_stream` 的 payload `model` 字段
- 模型列表 / Chat 快照仍使用内部展示 ID，对外自洽

**Mock 验证**：非流式 payload `model=deepseek-chat` ✅，流式 payload `model=deepseek-reasoner` ✅，其他模型原样 ✅

## 10.2 默认 Agent 更名 warm → general

遗留的 `warm` 默认值与预设体系（general/analyst/coder/writer）不一致，全局对齐为 `general`：

| 文件 | 字段 | 修改 |
|------|------|------|
| backend/app/api/settings.py | `DEFAULT_SETTINGS["default_agent"]` | `"warm"` → `"general"` |
| backend/app/models/agent.py | `Chat.agent_id` 列默认值 | `"warm"` → `"general"` |

**文档版本**: v42.0  
**最后更新**: 2026年7月31日（后端 AI · 天梁归档整理）

---

# 十一、V43 修复：P4 路径穿越校验 + P6 设置联动

**更新日期**: 2026年7月31日  
**版本**: v43.0

## 11.1 后端 (P4)：文件读取路径穿越拦截（backend/app/api/projects.py）

新增 `_resolve_safe_path(base_path, relative_path)` 安全解析函数：

- 拒绝绝对路径：`/` 开头、Windows 盘符（`C:\...`）
- `os.path.realpath` 解析后校验必须位于项目路径内，否则返回 400
- 拦截 `..` / `..\` / 深层 `../../` 相对穿越

生效位置：`read_file`（`GET /api/projects/{id}/file`）与 `list_project_files`（`GET /api/projects/{id}/files`）均改用该函数。

**Mock 验证**：`../`、`..\`、深层穿越、`/etc/passwd`、`C:\Windows\win.ini`、subpath 穿越 → 全部 400 拦截 ✅；正常文件与子目录读取 ✅

## 11.2 后端 (P6)：api_key 更新自动 Reload（backend/app/api/settings.py）

- `PUT /api/settings/{key}`：key 以 `api_key_` 开头时自动调用 `model_service.reload_models()`
- `PUT /api/settings`（批量）：包含任一 `api_key_*` 时同样触发 reload

避免用户修改 API Key 后还需手动调 `POST /api/models/reload`。

**Mock 验证**：PUT `api_key_deepseek` → reload 触发 ✅；PUT `theme` → 不触发 ✅；批量含 `api_key_qwen` → 触发 ✅

## 11.3 前端：ToolsPanel 对齐（frontend/src/components/panels/ToolsPanel.tsx）

- `GET /api/tools/definitions` 响应提取 `definitions` 数组（对齐后端返回结构）
- `POST /api/tools/call` 请求体对齐后端参数：`tool_name` + `arguments`
- 输出解析修正：成功读 `output`，失败读 `error`

**注**：ToolsPanel 改动实际提交于 `b8a7789`（V41 批次），本版本文档一并归档。

## 11.4 前端 (P4)：项目上下文面板（frontend/src/components/panels/ProjectContextPanel.tsx）

新增 `ProjectContextPanel` 组件并接入 Chat 界面：

- 聊天页头部新增「项目上下文」入口按钮（Folder 图标），打开右侧滑出面板
- 面板展示项目名称与路径，支持目录逐层浏览/返回（`GET /api/projects/{id}/files?subpath=...`）
- 文件 checkbox 勾选，底部已选文件列表支持单独移除与「全部清空」
- 发送消息时读取勾选文件内容（`GET /api/projects/{id}/file?path=...`），拼入 `[项目文件上下文]` 前缀带入对话
- 相关修改：`frontend/src/app/chat/[id]/page.tsx`（state、`buildContextPrefix`、`handleSend` 接入）

**验证**：文件列表/子目录/文件读取接口实测通过；lint 0 错误；`npm run build` 通过。

## 11.5 前端 (P6)：设置面板 API Key 批量保存 + 默认 Agent（frontend/src/components/panels/SettingsPanel.tsx）

- API Key 改为本地暂存输入 + 「保存」按钮批量提交 `PUT /api/settings`（body `{ settings: {...} }`），保存成功显示「已保存」反馈
- 新增「默认 Agent」下拉（读取 `settings.default_agent`，回退 `general`），保存 `default_agent`
- 相关修改：`frontend/src/lib/store.ts`（新增 `updateSettings` 批量方法）
- 新增 locales：`settings.ai.defaultAgent.*`、`settings.model.apiConfig.desc`、`common.saved/saving`、`chat.projectContext/selectedFiles/noFilesSelected`、`common.clear`

**验证**：批量 `PUT /api/settings` 实测生效（`default_agent=general`）；lint 0 错误；`tsc --noEmit` 通过。

**文档版本**: v43.0  
**最后更新**: 2026年8月1日（前端 AI 归档整理）

---

# 十二、V44 修复：Memory is_active 状态控制（P5）

**更新日期**: 2026年8月1日  
**版本**: v44.0

## 12.1 数据库与 Schema（backend/app/models/agent.py、main.py）

- `Memory` 模型新增 `is_active = Column(Boolean, default=True)`
- `main.py` 迁移 `_ensure_schema()` 扩展：旧库自动 `ALTER TABLE memories ADD COLUMN is_active BOOLEAN DEFAULT 1`
- 前端 P5 契约对齐：`memory_type` 兼容 `user` / `project` / 旧版 `preference`（前端创建默认传 `preference`）

## 12.2 API（backend/app/api/memory.py）

- 新增 `PATCH /api/memory/{id}` 更新接口（前端 useMemory 调用 `apiPatch`），与 `PUT` 共享 `_apply_update` 实现
- `MemoryResponse` / `MemoryCreate` / `MemoryUpdate` 均含 `is_active`
- POST upsert 与 PUT/PATCH 均可读写 `is_active` 状态

## 12.3 System Prompt 注入过滤（backend/app/api/chat.py）

- `_get_memory_prompt` 用户记忆与项目记忆查询均加 `Memory.is_active == True` 过滤
- 禁用（`is_active == False`）的记忆条目不会注入 System Prompt

**实测验证**（TestClient + mock）：
- `PATCH /api/memory/{id}` 发送 `{"is_active": false}` → 200，数据库记录实际更新 ✅
- `PATCH` 恢复 `true` ✅
- 前端契约 `memory_type="preference"` 创建通过 ✅
- Prompt 注入：active 记忆注入、inactive 记忆排除 ✅

**相关提交**：
- `d4f4d41` feat(frontend): memory panel categories + CRUD + enable toggle (P5)
- `e95046f` feat(backend): add is_active field to memory schema, PATCH endpoint, and prompt filter (P5)

**文档版本**: v44.0  
**最后更新**: 2026年8月1日（后端 AI 归档整理）

---

# 十三、V45 修复：模型继承 401 + Agent 浮层恢复 + 人格滑块优化

**更新日期**: 2026年8月1日  
**版本**: v45.0

## 13.1 修复：首页模型选择继承失效（401 根源）

**根因**：首页 `handleSend` 创建 Chat 时未传递 `model` → `chat.model` 为 null → 聊天页 `currentModel = models[0]`（mimo 在模型列表首位）→ 用户选中的 DeepSeek 被降级为 mimo，触发旧模型链路 401。

**修复**（前后端联动）：
- 后端 `ChatResponse` 增加 `model` / `personality_level` 字段（列表、单查均返回）
- 后端 `ChatUpdate` 支持更新 `model` / `personality_level`（PATCH 持久化）
- `useChat.createChat` 支持传 `model`；首页 `handleSend` 传 `currentModel.id`（含默认模型回退）
- 聊天页 `currentModel` 优先级：`selectedModel → currentChat.model → models[0]`
- 聊天页切换模型时 PATCH 持久化到 Chat，下次进入仍继承

**验证**：POST 创建传 model 正确返回、GET 列表/单查/更新均含 model/personality ✅

## 13.2 恢复：createPortal 浮层 Agent 选择器

**根因**：`b42d1f2`（含下拉实现）后 `page.tsx` 被覆盖为"点击循环切换"版本，浮层选择器遗失。

**修复**：恢复 `createPortal` 浮层实现（`agent-dropdown-portal`），包含：
- AGENT_COMBOS 名称映射（general→小暖 / analyst→锐 / coder→码农 / writer→笔神）+ 推荐人格
- 点击外部关闭、定位到按钮下方、选中态高亮
- 适配当前 `useSettingsStore` 与 `createChat` 签名

## 13.3 优化：PersonalitySlider 消除拖动闪烁

**根因**：滑块 `onChange` 直接 setState，拖动全程重渲染整个 ChatPage。

**修复**：抽取 `PersonalitySlider` 子组件：
- 拖动时仅更新子组件内部 `draft` state（父级不重渲染）
- 松手（onPointerUp）/ 失焦（onBlur）/ 回车（onKeyUp）时 commit 到父级并 PATCH 持久化 `personality_level`

## 验证

- `tsc --noEmit` ✅ 0 错误
- `eslint` ✅ 本次修改文件 0 错误（4 个 error 为 electron/*.js 历史遗留 require，非本次）
- 后端 Chat model/personality API 实测 4 项 ✅

**相关提交**：`18d61b8` fix: resolve model inheritance 401 bug, restore agent dropdown, and optimize personality slider re-render

**文档版本**: v45.0  
**最后更新**: 2026年8月1日（开发 AI 归档整理）

---

# 十四、V46 改造：人格继承修复 + Memory 嵌入设置 + 思考模式

**更新日期**: 2026年8月1日  
**版本**: v46.0

## 14.1 修复：老会话人格继承（frontend/src/app/chat/[id]/page.tsx）

- 人格初始值改为**优先读取 `currentChat.personality_level` 会话快照**，仅当其 null/undefined 时回退 `settings.default_personality` / 50
- 新增 `personalityInitForChatId` 跟踪，**切换会话时重新初始化**，修复进入老会话恢复全局默认值的 Bug
- 老会话各自继承自己保存的理性度

## 14.2 优化：Memory 面板嵌入设置（MemoryPanel / SettingsPanel / Sidebar / AppLayout）

- `MemoryPanel` 新增 `embedded` prop，嵌入模式渲染无外框内容
- 《设置面板》【长期记忆】标签页内嵌 `<MemoryPanel embedded />`
- 移除侧边栏独立 Memory 入口（`onMemoryClick`）与 AppLayout 全局 MemoryPanel 渲染

## 14.3 重构：Tools 面板 → 思考模式选择器（前端 + 后端）

- **废弃手动 `ToolsPanel`**（已备份至 `_trash/ToolsPanel.tsx`，可回滚；`.gitignore` 新增 `_trash/` 避免备份污染历史）
- Chat 界面工具栏新增【思考模式】分段选择器：**关闭 / 快速推理 / 深度思考** → `none | low | high`
- `useMessages.sendMessageStream/sendMessage` 新增 `reasoningEffort` 参数，随 Payload 透传 `reasoning_effort`
- 后端 `SendRequest` 新增 `reasoning_effort`；`model_service.chat/chat_stream` 支持该参数，非 `none` 时写入 API payload 的 `reasoning_effort` 字段
- locales 新增 `chat.reasoning.{title,off,fast,deep}`（zh-CN / en-US）

## 14.4 修复：autoSend 模型硬编码（frontend/src/app/chat/[id]/page.tsx）

- 修正 `autoSend` 误将模型硬编码为 `mimo-v2.5-pro` 的问题 → 改为使用 `currentModel?.id`，并同步传 `reasoningEffort`

## 验证

- `tsc --noEmit` ✅ 0 错误
- `eslint` ✅ 本次修改文件 0 问题（仅 electron/*.js 历史遗留）
- 后端 reasoning_effort 链路实测 5 项 ✅（send 传递 / None 兜底 / payload 写入 / none 省略 / 流式传递）

**相关提交**：`26160c2` feat: embed memory in settings, replace tools with thinking mode, and fix chat personality inheritance

**文档版本**: v46.0  
**最后更新**: 2026年8月1日（后端 AI 归档整理）

---

# 十五、V47 优化：设置面板布局 + Agent 面板紧凑化 + 前端 UI Agent

**更新日期**: 2026年8月1日  
**版本**: v47.0

## 15.1 优化 (前端)：设置面板左右布局隔离（frontend/src/components/panels/SettingsPanel.tsx）

- 左侧导航固定不滚动，右侧设置内容独立滚动
- `height: 100%` + `overflow: hidden`（左侧）/ `overflowY: auto`（右侧），长内容不再撑破面板

## 15.2 优化 (前端)：Agent 下拉/抽屉紧凑化（frontend/src/app/page.tsx）

- 压缩 `AGENT_COMBOS` 下拉的内边距（`7px/10px` → `5px/8px`）、行高与字号
- 下拉 `maxHeight` 260px → 220px，`padding` 4px → 3px，提升 UI 视觉密度
- 名称映射更新：`coder` → 代码审查 AI、`frontend_ui` → 前端 UI 设计 AI、`backend` → 后端 AI

## 15.3 新增 (前后端)：前端 UI Agent 预设 + 后端显式排序（seed_agents.py / agents.py）

- 新增 `frontend_ui`（前端 UI 设计 AI）、`backend`（后端 AI）预设；`coder` 定位升级为"代码审查 AI"
- 后端 `GET /api/agents` 新增 `AGENT_ORDER` + `ORDER BY CASE` 显式排序：
  `general → frontend_ui → backend → analyst → coder → writer → 其他(99)`
- 确保【研发核心三剑客】general / frontend_ui / backend 稳定置顶，不受数据库插入顺序影响

**验证**：`GET /api/agents` 实测返回顺序 `['general', 'frontend_ui', 'backend', 'analyst', 'coder', 'writer', 'warm', 'rational']` ✅；`frontend_ui`/`backend` identity 完整 ✅

**相关提交**：`07d4824` feat: optimize settings scroll layout, compact agent UI, and introduce Frontend UI Agent (V47.0)

**文档版本**: v47.0  
**最后更新**: 2026年8月1日（后端 AI 归档整理）

---

# 十六、V48 重构：Agent 图标高级化 + 语义 ID 归一化

**更新日期**: 2026年8月1日  
**版本**: v48.0

## 16.1 优化 (前端)：AgentIcon Lucide 线条图标（frontend/src/components/AgentIcon.tsx）

- 新增 `AgentIcon.tsx` 组件，使用 `lucide-react`（`strokeWidth={1.75}`）极简线条图标
- 按 agent.id 映射：`coder/reviewer`→Code2、`frontend_ui`→Palette、`backend`→Server、`general`→Sparkles、`analyst`→BarChart2、`writer`→PenTool；未知 id 回退 Bot
- 全量替换 8 处 Emoji 头像：首页 Agent 下拉（按钮 + 选项）、Chat 页头部/消息、MemoryPanel Agent 选择、SettingsPanel 默认 Agent 与预设列表
- 顺带优化：`PersonalitySlider` 加 `React.memo` + `useCallback` + `draftRef`，拖动完全隔离父组件重渲染

## 16.2 重构 (后端)：avatar/icon 语义 ID 归一化（seed_agents.py / agents.py）

- `seed_agents.py` 预设 Agent `avatar` 由 Emoji 改为纯 ASCII 语义 ID：`code` / `palette` / `server` / `sparkles` / `search` / `pen`
- 新增遗留 Emoji 归一化清洗逻辑：`_contains_emoji`（非 ASCII 检测）自动将旧记录 `warm`→`heart`、`rational`→`target` 归一化，数据库无残留 Emoji
- `agents.py` 的 `AgentInfo` 新增 `icon` 字段（= 语义 ID），`GET /api/agents` 与 `GET /api/agents/{id}` 均返回

**验证**：`GET /api/agents` 全部 `icon` 为纯 ASCII 语义 ID（含遗留 warm/rational）✅；`tsc --noEmit` 0 错误 ✅；eslint 本次文件 0 问题 ✅

**相关提交**：`9952cc7` feat: replace agent emoji avatars with Lucide line icons and update backend semantic IDs (V48.0)

**文档版本**: v48.0  
**最后更新**: 2026年8月1日（后端 AI 归档整理）

---

# 十七、V49 重构：ChatInput 一体化 + 首页草稿预挂载 + 项目绑定 API

**更新日期**: 2026年8月1日  
**版本**: v49.0

## 17.1 重构 (前端)：ChatInput 一体化卡片（frontend/src/components/ChatInput.tsx）

- 输入框与底部工具栏重构为**单卡片结构**（首页 + Chat 页共用）
- 微型化 `+` 旋转菜单 / 模型选择 / 思考胶囊
- 彻底清理 Chat Header 的人格滑块（改由 ChatInput 卡片承载）

## 17.2 新增 (前端)：首页草稿预挂载（frontend/src/app/page.tsx + FileDropZone.tsx）

- 首页支持 `pendingProject` / `pendingFiles` 草稿预挂载
- 带文件/项目一键创建会话（`FileDropZone` 拖拽 + 目录选择 IPC）

## 17.3 新增 (后端)：项目绑定 API（projects.py / chat.py / agent.py / main.py）

- `POST /api/projects`：本地路径安全校验（绝对路径 + 目录存在 + 非文件），`name` 缺省自动提取目录最后层级，同路径去重
- `PATCH /api/chat/{id}`：支持 `project_id` 绑定与 `unbind_project` 解绑，绑定后自动继承 `project_path` Context 目录权限
- `Chat` 模型新增 `project_path` / `context_files` 字段（含迁移）；`GET /api/chat` 返回分组信息
- 拖拽文件 API：`GET/POST/DELETE /api/chat/{id}/context_files`，路径必须位于项目目录内（防穿越）

**验证**：项目创建（自动命名/去重/越权 400/不存在 404）、Chat 绑定解绑、context_files 追加/去重/越权拦截，全部实测通过 ✅；`tsc --noEmit` 0 错误 ✅

**相关提交**：`8351675` feat: integrate ChatInput card, support homepage draft context staging, and implement project binding APIs (V49.0)

**文档版本**: v49.0  
**最后更新**: 2026年8月1日（后端 AI 归档整理）
