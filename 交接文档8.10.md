我把这份 MfkAgent 交接文档压缩成“接手 AI 能快速理解”的版本：



# MfkAgent 项目交接总结（v85.0）

---

## 📝 前端 AI 交接记录（2026-08-11）：侧边栏双竖杠修复

**【完成内容】**：修复侧边栏选中项目内会话时同时出现两条 2px 左侧指示条（近黑主题下视觉为“两条黑色大竖杠”）的问题。根因：`activeProjectId` 派生自 `currentChatId`，父子两层必然同时激活，各自渲染一条指示条。修复策略：项目级指示条**仅在项目折叠时渲染**（此时子级不显示，它是唯一位置信号）；展开时由子级会话的指示条承担标记，父行保留底色 + 前景色做层级表达。未改动颜色变量（黑色来自主题主色 `--mf-accent`，属设计预期）。

**【修改文件】**：
- `frontend/src/components/sidebar/ProjectNode.tsx`（渲染条件 `isActive` → `isActive && isCollapsed`，同步更新 V77 设计规则注释）
- `ChatRow.tsx` 未改动
- 备份：`_backup/sidebar_double_indicator_20260811/`（可随时回滚）

**【后端接口依赖】**：无，纯前端 UI 调整

**【UI 变化对比】**：
| 场景 | 修复前 | 修复后 |
|---|---|---|
| 项目展开 + 会话选中 | 父行与子行各一条竖杠（双杠） | 仅子级会话一条竖杠 |
| 项目折叠 + 会话在其中 | 父行一条竖杠 | 父行一条竖杠（不变） |

---

## 📝 前端 AI 交接记录（2026-08-11）：字体本地化（构建零外网依赖）

**【完成内容】**：消除构建期对 Google Fonts 的联网依赖。原 `next/font/google` 在构建期需联网下载 7 款字体做自托管，离线/网络抖动时构建直接崩。改造方案：
- 单文件 Latin 字体（Geist / Geist Mono / VT323）→ `next/font/local` + `public/fonts/*.woff2`（从 @fontsource 包拷入）
- unicode-range 分片字体（Noto Sans SC 101 片 / IBM Plex Sans / Press Start 2P / DotGothic16）→ fontsource 包 CSS 直引（npm 自带 @font-face，构建零网络）
- 所有 `--font-*` CSS 变量名保持不变，主题/组件/字体切换逻辑零改动
- providers.tsx 字体检测名同步为 fontsource 注册的 family（`Noto Sans SC Variable` / `IBM Plex Sans Variable`），避免误判本地缺失而误走 CDN

**【修改文件】**：
- `frontend/src/app/layout.tsx`（google → local + fontsource CSS 直引）
- `frontend/src/app/globals.css`（:root 补定义 4 个 fontsource 字体变量）
- `frontend/src/components/providers.tsx`（FONT_CHECK_NAME 同步 Variable 后缀）
- `frontend/src/components/panels/BasicSettingsView.tsx`（仅注释）
- `frontend/public/fonts/`（新增 4 个 woff2，共 82KB）
- `frontend/package.json`（新增 7 个 @fontsource 依赖）
- 备份：`_backup/font_localization_20260811/`

**【后端接口依赖】**：无，纯前端改造

**【验证结果】**：`.next` 清空后全新构建通过；产物 234 个 woff2 全部本地；HTML/CSS 无 fonts.googleapis/gstatic 引用；7 个 --font-* 变量在产物中均存在。已知影响：产物体积 +5.3MB（CJK 分片字体，按需加载不阻塞首屏）。

---

## 一句话定位

MfkAgent 已经从“聊天壳子”进化成一个本地化 AI Agent 平台：

核心链路：

**用户 → Chat → AgentRuntime → Planner/TaskGraph → Tool执行 → Verification → Memory沉淀**

目前 V1 主体已经完成，正在进入“能力增强 + 产品打磨 + 稳定交付”阶段。

---

# 一、当前完成状态

## 已完成核心模块

### 1. 基础产品闭环 ✅

已经完成：

* Chat 聊天系统
* Agent 体系
* Project 工作区
* Memory 记忆系统
* Settings 配置系统

Agent 目前：

* 6 个正式 Agent：

  * 通用
  * 前端 UI
  * 后端
  * 分析师
  * 代码审查
  * 笔神

另外保留：

* warm
* rational

两个旧兼容 Agent。



---

# 二、AI Runtime 核心架构

这是项目最重要部分。

## 1. Model Service

已经完成模型服务重构：

支持：

* 12 家 Provider
* OpenAI 兼容接口
* 自定义模型接入
* API Key 管理
* 模型能力检测

默认模型：

qwen-flash。

---

## 2. AgentRuntime

现在所有 Agent 执行统一入口：

```
用户请求
 ↓
AgentRuntime
 ↓
工具调用
 ↓
验证
 ↓
结果返回
```

支持：

* 流式输出
* 非流式调用
* 权限控制
* 自检 Verification

---

## 3. Planner + TaskGraph

已经实现：

### Planner

负责：

* 任务拆解
* 生成执行计划

如果模型无法规划：

自动降级：

Heuristic Fallback（规则规划）。

---

### TaskGraph

负责：

* DAG任务图
* 节点状态
* 依赖管理
* 失败传播

支持：

失败节点自动跳过后续依赖。

---

## 4. 动态自愈

Agent 执行失败后：

```
失败
 ↓
Reflection
 ↓
分析原因
 ↓
动态插入修复节点
 ↓
继续执行
```

也就是说 Agent 已经具备一定自主修复能力。

---

# 三、Prompt 系统

System Prompt 已经标准化：

8层结构：

```
身份
 ↓
能力
 ↓
执行规范
 ↓
权限
 ↓
模式
 ↓
项目上下文
 ↓
人格
 ↓
任务上下文
```

目标：

让 Agent 不再依靠一大坨 Prompt，而是动态组合。

---

# 四、近期重要修复

## 1. API Key逻辑调整

之前：

所有 Key 强制脱敏。

现在：

因为 MfkAgent 定位是本地工具，所以改为：

用户自己拥有完全控制权。

修改：

* 去除 API Key 脱敏逻辑
* 删除废弃 MCP / Workflow / AutoTask 路由

接口保持兼容。



---

## 2. 附件系统完成

现在支持：

用户上传：

* 文本
* 图片
* 二进制文件

流程：

```
上传文件
 ↓
Attachment
 ↓
Context Builder
 ↓
Agent理解
 ↓
模型调用
```

文本：

直接读取内容。

图片：

进入 Vision 路由。

二进制：

提供文件信息，让 Agent 调工具读取。



---

## 3. 多模态 Vision

已经支持：

图片输入：

```
图片附件
 ↓
判断模型是否支持 Vision
 ↓
支持:
直接发送图片

不支持:
备用识图模型解析
 ↓
文本注入
```

支持：

* Gemini
* Qwen-VL
* GLM



---

## 4. Agent 自治模式

新增：

`auto_approve`

目标：

减少用户频繁点击确认。

权限分级：

```
ALLOW
 ↓
直接执行


REQUIRE_APPROVAL
 ↓
自动模式可执行


HIGH_RISK
 ↓
永远人工确认


DENY
 ↓
禁止
```

高危操作不能绕过。



---

# 五、桌面端状态

Electron 已经修复。

之前问题：

1. Electron 二进制下载失败
2. Next.js 配置导致前端500

已经解决：

现在链路：

```
启动脚本
 ↓
Backend
 ↓
Frontend
 ↓
Electron
```

可以完整启动。



---

# 六、当前剩余问题 / 下一阶段

重点不是继续堆功能，而是增强体验。

主要方向：

## 1. Memory 2.0

升级：

从简单存储：

↓

认知记忆系统。

目标：

让 Agent 真正长期理解用户。

---

## 2. Agent状态可视化

现在已有：

状态事件。

下一步：

展示：

```
规划中
 ↓
拆任务
 ↓
执行工具
 ↓
验证
 ↓
完成
```

类似 Claude Code 的执行体验。

---

## 3. Timeline

增加：

Agent执行过程时间线。

让用户知道：

“AI到底干了什么”。

---

## 4. Agent能力增强

当前反馈：

“Agent还不够强”。

下一步：

重点提升：

* 任务理解
* 规划质量
* 工具调用能力
* 自我修复能力

---

# 七、当前接手 AI 应该注意

不要做：

❌ MCP生态
❌ Plugin市场
❌ Workflow编辑器
❌ AutoTask平台化

这些已经归档，不符合产品方向。

应该继续：

✅ Agent能力
✅ Memory
✅ 执行体验
✅ 桌面产品体验
✅ 稳定性

---

## 当前项目评价

现在的 MfkAgent 已经不是“套 API 的聊天软件”。

核心框架已经搭起来：

* 有 Agent
* 有 Runtime
* 有工具
* 有规划
* 有权限
* 有记忆
* 有多模态
* 有桌面端

下一阶段重点应该从“造能力”转向：

**让用户感觉它像一个真正工作的 AI 助手。** 🙂


---

# 交接记录：历史消息时序渲染 — 消费后端 timeline 字段，修复工具卡片流结束后上浮（2026-08-10，前端 AI）

## 问题背景

Agent 调用工具时流式期间一切正常（工具卡片按 SSE 到达顺序穿插在文本流中），但 Agent 输出结束瞬间，所有调用过的工具卡片统一上浮到消息体最上方。根因：流结束时 appendAssistant 清空流式 timeline 并以三字段（content/thinking/tool_calls）重组消息，ChatMessage 历史消息硬编码固定渲染顺序 tool_calls → thinking → 正文，时序信息丢失。调查发现后端 Message.timeline JSON 列、流式录制与 MessageResponse.timeline 下发链路均已就绪（backend/app/api/chat.py L746-808），前端此前从未消费。

## 完成内容

1. Message 接口新增 timeline 字段：新增 TimelineEvent 类型定义，承接 GET /api/chat/{id}/messages 已下发的时序事件。
2. ChatMessage 时序渲染：新增 buildTimelineSegments 转换函数（连续 thinking 合并、tool_start/tool_result 按 tool_call_id 配对成单张卡片且完整态优先取 message.tool_calls、text 渲染 Markdown）。AI 消息有 timeline 时按时序交错渲染；无 timeline 的旧消息回退原固定顺序；timeline 缺 text 事件但正文存在时补渲染正文防内容丢失。
3. 流结束零跳变：appendAssistant 在清空流式 timeline 前快照并转换为后端一致的事件格式随乐观消息写入，流结束瞬间即按时序渲染，refetch 落地后无缝衔接。

## 修改文件

| 文件 | 说明 |
|------|------|
| frontend/src/hooks/useMessages.ts | 新增 TimelineEvent 类型；Message 接口增 timeline 字段 |
| frontend/src/components/ChatMessage.tsx | 新增 buildTimelineSegments；AI 消息优先按 timeline 时序渲染，无则回退固定顺序；另将 normalizeThinking 正则改为等价无转义写法（行为不变） |
| frontend/src/hooks/useChatStream.ts | appendAssistant 快照流式 timeline 转事件格式写入乐观消息 |

## 后端接口依赖

无新增需求。依赖既有 GET /api/chat/{id}/messages 返回的 MessageResponse.timeline（后端已实装，无需改动）。

遗留建议（非阻塞，供后端 AI 评估）：当前后端将所有文本合并为单个 text 事件追加在 timeline 末尾，"文本→工具→文本"交错场景的历史态文本分段时序仍无法重建。如需完美重建，建议后端在 tool_start 事件发生时先落一段 text 事件（分段记录）。工具卡片位置不受此影响，已正确还原。

## UI 变化对比

- 流式期间：无变化（仍按 timeline 真实顺序渲染）。
- 流结束/历史消息：工具卡片不再统一上浮，保持调用时的真实位置（思考→工具→文本交错）；timeline 落库前的旧消息保持原固定顺序不变。
- 思考面板：timeline 渲染下多轮思考按被隔开的位置分段展示，persistKey 追加段序号避免折叠状态串扰。

## 验证结果

- npx tsc --noEmit 全量类型检查通过（0 error）
- 回退路径保留：无 timeline 字段的消息渲染逻辑与改动前完全一致

## 安全与回滚

- 修改前备份：_backup/timeline_render_fix_20260810/（useMessages.ts / ChatMessage.tsx / useChatStream.ts 三个原始文件 + 本文档追加前副本）
- 回滚方式：将备份文件覆盖回原路径即可
- 未触碰：后端代码、数据库结构、MessageList 流式渲染区、ToolCallCard 组件

---

# 交接记录：后端 timeline 录制修复 — text 分段落盘，保留文本/工具真实交错时序（2026-08-10，前端 AI 经用户授权跨域执行）

## 授权说明

按企划书职责边界，backend/app/api/chat.py 属后端 AI 领域。经用户明确授权（"先审查一遍自己的方案，然后在保证可以回滚的前提下执行"），由前端 AI 执行本最小改动，请后端 AI 知悉并复核。

## 问题背景

用户反馈工具卡片在 Agent 输出结束后仍上浮。取证（消息 id=895）：流式期间真实时序为 text1（第1轮）→ tool → tool → text2（第2轮），但 DB timeline 录制结果为 tool_start/tool_result × 2 → text（全部文本合并在末尾）。根因：_background_agent 录制循环中 text chunk 只累加 full_content 不记录，流结束才把全部文本合并为单个 text 事件追加末尾。前端（ChatMessage timeline 时序渲染，见上一条交接记录）忠实渲染后端时序，故工具仍显示在文本之前。

## 完成内容

app/api/chat.py 流式录制循环改动（约10行）：
1. 新增 pending_text 缓冲：text chunk 同时累加 full_content 与 pending_text。
2. tool_start/tool_result/tool_approval 事件落 timeline 前，先把 pending_text 以独立 text 事件 flush 落盘——timeline 由此保留"文本→工具→文本"真实交错。
3. flush 收口在 _persist() 内（nonlocal pending_text），正常完成/取消/异常三条落库路径统一覆盖；取消场景的残余文本顺带补全（旧代码该场景会丢最终 text）。

## 行为等价性论证（自审结论）

- 无工具场景：缓冲仅在落库前 flush 一次，产物与旧行为完全一致（单个 text 事件在末尾）。
- full_content / tool_calls / Message.content 落库逻辑零改动，仅 timeline 展示层数据变化。
- tests/test_timeline_persistence.py 断言兼容：该测试仅断言"包含 text/tool_start/tool_result"与 tool_result 跟随 tool_start，未断言 text 必须在末尾；且为需真实服务的验收脚本，非 pytest 套件。
- 前端 ChatMessage.buildTimelineSegments 已支持多 text 段交错渲染，无需改动。
- 历史消息不受影响（旧数据保持旧时序渲染回退路径不受影响）。

## 修改文件

| 文件 | 说明 |
|------|------|
| backend/app/api/chat.py | _background_agent 录制循环：pending_text 缓冲 + 工具事件前 flush + _persist 内收口 flush |

## 验证结果

- python -m py_compile app/api/chat.py 通过
- from app.api import chat 导入通过
- E2E 待用户重启应用后验证（后端进程 PID 24520 于 21:24 启动，早于本次改动，需重启加载）

## 对前端的影响

无需改动。重启后端后新消息的 timeline 即为真实时序，前端按时序渲染，工具卡片不再上浮。

## 安全与回滚

- 修改前备份：_backup/timeline_record_fix_20260810/chat.py.bak（另附本文档追加前副本 .bak2）
- 回滚方式：chat.py.bak 覆盖回 backend/app/api/chat.py 并重启后端
- 未触碰：AgentRuntime / Tool Runtime / TaskGraph / 权限模型 / 数据库结构 / SSE 转发逻辑（_put 与 generate 未动）

---

# 交接记录：消息通知系统优化 — AppUserModelId + Toast 节流 + 触发条件一致性（2026-08-10，前端 AI）

## 完成内容

1. Windows 通知身份修复（electron/main.js）：在 app.whenReady() 前（单实例锁逻辑之后）调用 app.setAppUserModelId("com.mfkagent.app")，与 electron-builder.yml 的 appId 保持一致，仅 win32 平台执行。修复 Toast 归属 "Electron" 默认身份导致的通知无应用名/图标问题。
2. Toast 节流（src/lib/notify.ts）：showDesktopNotification 入口新增 3 秒节流（TOAST_THROTTLE_MS，与提示音节流同级），窗口内重复通知直接丢弃（含提示音），无队列。防止多任务同时完成时右下角堆叠大量 Toast。
3. 触发条件一致性（src/hooks/useChatStream.ts）："需要审批"与"任务出错"两处系统通知补上 activeChatIdRef.current !== targetChatId 守卫，与"任务完成"/"任务失败"通知一致——用户正在查看该会话时界面已有审批卡片/错误卡片，不重复弹系统通知；离开会话时照常通知。

## 修改文件

| 文件 | 说明 |
|------|------|
| frontend/electron/main.js | 新增 app.setAppUserModelId（whenReady 前、win32 条件） |
| frontend/src/lib/notify.ts | 新增 lastToastTime/TOAST_THROTTLE_MS，入口 3 秒节流 |
| frontend/src/hooks/useChatStream.ts | 审批通知 + 流式错误通知补活跃会话守卫 |

## 后端接口依赖

无。未触碰后端/Runtime/数据库。

## UI 变化对比

- 通知外观：Windows Toast 归属显示为 MfkAgent 应用身份（打包版生效；开发模式 Electron 进程身份仍受宿主 exe 限制属预期）。
- 通知频率：3 秒内最多一条 Toast；用户在场查看会话时审批/错误不再弹系统通知（界面内卡片照常显示）。

## 验证结果

- node --check electron/main.js 通过
- npx tsc --noEmit 全量类型检查通过（0 error）

## 安全与回滚

- 修改前备份：_backup/notify_optimize_20260810/（notify.ts / main.js / useChatStream.ts）
- 回滚方式：备份覆盖回原路径；main.js 改动需重启应用生效，渲染层改动需重新构建 out/

---

# 交接记录：通知触发条件重构 + 侧边栏转圈修复 + 任务用时展示（2026-08-10，前端 AI）

## 背景

用户实测反馈：任务完成无任何通知（无 Toast、无提示音）；后台任务完成后左侧会话转圈不停，点进去才恢复。需求新增：AI 回复展示本次任务用时。

## 根因（审查结论）

1. 通知守卫只判断 activeChatIdRef.current !== targetChatId：用户停留在该会话但切到其他窗口/最小化时不弹（提示音同函数内，也没响 → 证明函数根本没被调用）。
2. activeChatIdRef 位于 chat/[id]/page.tsx 组件内：用户离开聊天页后组件卸载，ref 冻结为发送时的 chatId，流结束时仍误判"用户在场"→ 永不弹。
3. 侧边栏加载指示器 streams[chatId] 仅由页面组件 effect 清理：后台流结束无人清理 → 一直转圈，重进会话才恢复。

## 完成内容

1. streamStore.ts：新增全局 activeChatId / setActiveChatId（不参与 persist）。
2. chat/[id]/page.tsx：挂载/切换时同步全局 activeChatId，卸载置 null。
3. useChatStream.ts：新增 isUserAway() 三重判定（全局活跃会话 !== 目标会话 OR document.visibilityState !== visible OR !document.hasFocus()），替换全部 4 处通知守卫；记录 startedAt，任务完成通知正文附"· 用时 X 秒"。
4. 转圈修复：appendAssistant 与 resetStreaming 中主动 store.setStream(targetChatId, null)。
5. 用时展示（纯前端）：lib/format.ts 新增 formatDuration；MessageList 按"AI 消息 created_at − 前置用户消息 created_at"计算 durationMs（乐观消息与历史消息均成立，两端皆本机时钟）；ChatMessage 操作栏左侧低调展示"用时 X 秒"。
6. notify.ts：触发/节流均输出 console.log（"[notify] 触发通知" / "[notify] 节流中"）便于验证。

## 修改文件

frontend/src/lib/streamStore.ts、frontend/src/app/chat/[id]/page.tsx、frontend/src/hooks/useChatStream.ts、frontend/src/lib/format.ts（新增）、frontend/src/components/MessageList.tsx、frontend/src/components/ChatMessage.tsx、frontend/src/lib/notify.ts

## 后端接口依赖

无。未触碰后端/Runtime/数据库。

## 验证结果

- npx tsc --noEmit：本次改动 6 文件 0 错误（ExtensionPanel.tsx 2 个 TS2554 为其他并行任务 22:56 编辑中的中间态，与本改动无关）
- 运行模式确认：start-desktop.bat 为 ELECTRON_DEV + next dev 热更新，渲染层改动重启应用即生效，无需 npm run build

## 安全与回滚

- 修改前备份：_backup/notify_trigger_fix_20260810/（6 文件）
- 回滚方式：备份覆盖回原路径，重启应用

---

# 交接记录：Windows dev 模式 Toast 静默丢弃修复（开始菜单 AUMID 快捷方式补建）（2026-08-10，前端 AI）

## 背景

上一轮修复后用户实测：提示音正常、控制台出现 [notify] 触发通知 日志，但 Windows 右下角 Toast 仍不显示 → 触发链路已通，问题在 OS 层。

## 根因

Windows 10/11 要求 Toast 的 AppUserModelId 必须对应一个带 System.AppUserModel.ID 属性的开始菜单快捷方式，否则通知被系统静默丢弃（Electron 官方文档确认）。打包版由 NSIS 安装器自动创建该快捷方式；dev 模式（npx electron .）无安装过程 → 被丢弃。

## 完成内容

1. 新增 frontend/electron/dev-notification-shortcut.ps1：用 IShellLinkW + IPropertyStore COM 接口创建开始菜单快捷方式 "MfkAgent Dev.lnk"，目标指向 electron.exe，写入 PKEY_AppUserModel_ID = com.mfkagent.app。
2. electron/main.js：新增 ensureDevNotificationShortcut()，app.whenReady() 内最先执行；仅 win32 + ELECTRON_DEV=true 且快捷方式不存在时异步补建（windowsHide、失败仅告警不阻断启动）。
3. 已在当前机器验证创建成功：C:\Users\Asus\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\MfkAgent Dev.lnk（1195 字节，含属性存储）。

## 踩坑记录

- IPersistFile 的 COM vtable 声明必须包含 IPersist.GetClassID；省略后 vtable 错位，Save 实际调用 Load，返回 S_OK 但文件不落盘（已修正并注释）。
- 调试期间在开始菜单留下一个 "MfkAgent Dev Test.lnk" 对照测试残留（无 AUMID、无害），工具限制无法代删，已告知用户可手动移除。

## 修改文件

frontend/electron/dev-notification-shortcut.ps1（新增）、frontend/electron/main.js

## 验证结果

- node --check electron/main.js 通过
- 脚本实机运行：SHORTCUT_OK，文件落盘确认（Test-Path 验证）

## 安全与回滚

- main.js 备份：_backup/notify_trigger_fix_20260810/main.js.bak2
- 回滚方式：恢复备份 + 删除开始菜单中的 "MfkAgent Dev.lnk"；脚本为新增文件，直接删除即可
- 未触碰后端/Runtime/数据库；打包版行为完全不受影响（ensureDevNotificationShortcut 非 dev 环境直接 return）
---

# 追加记录：字体设置全面生效修复（表单控件继承 + 自定义字体选择器 + 本地自托管字体）（2026-08-11，前端 AI）

## 背景

用户反馈设置中选择 IBM Plex Sans 后"看不出变化"。审查确认三层问题：
1. 浏览器 UA 规定表单控件（button/input/select/textarea）不继承 body 字体，交互密集区全部渲染 Arial，字体设置在这些区域完全无效（实测：首页 16 个可见文本元素为 Arial）；
2. 字体选择器为原生 <select>，其 option 在 Windows 上无法应用字体，选择界面无法所见即所得；
3. FontProvider 运行时注入 Google Fonts CDN（国内不稳定），而 next/font 已在构建期将 IBM Plex Sans / Noto Sans SC 下载自托管（out/ 内 23 条 @font-face，离线可用），形成重复加载与网络依赖。

## 修改内容

1. frontend/src/app/globals.css：新增 `button, input, select, textarea, optgroup { font-family: inherit }` 全局规则，交互控件统一跟随 --font-family；组件级显式内联 fontFamily 优先级更高，不受影响。
2. frontend/src/components/providers.tsx：FONT_FAMILY_MAP 改为导出；source-han-sans / ibm-plex-sans 直接引用 next/font 自托管变量 var(--font-noto-sans-sc) / var(--font-ibm-plex-sans)，fontCDN 仅保留霞鹜文楷（无本地托管），去除 Google Fonts 运行时依赖。
3. frontend/src/components/panels/BasicSettingsView.tsx：原生 <select> 替换为自定义 FontFamilyDropdown（createPortal 弹层，复用 GroupedModelDropdown 的视觉与交互模式：点外关闭/Esc 关闭/选中打勾）；每个选项名以其本尊字体渲染；下拉打开时预载霞鹜文楷 webfont 供预览（独立 link id=font-preview-cdn）。
4. 同文件字体区下方新增实时预览段：英文 The quick brown fox… + 中文 你好，世界！以当前所选字体实时渲染。

## 验证结果（浏览器实测 localhost:3000）

- 修复前：IBM 50 个元素 / Arial 16 个元素；修复后：IBM 66 / Arial 0，button 与 input 计算字体均为 IBM Plex Sans。
- font-cdn link 已消失（不再请求 Google Fonts），字体来自本地自托管。
- 下拉弹层 4 个选项各以本尊字体渲染（系统默认/Segoe UI、Noto Sans SC、LXGW WenKai、IBM Plex Sans 均已实测确认）。
- 端到端切换：选"思源黑体"→全局 --font-family 即时变为 Noto Sans SC（中文字形随之改变）；切回 IBM Plex Sans 恢复原设置。
- npx tsc --noEmit 通过（exit 0）。

## 修改文件

frontend/src/app/globals.css、frontend/src/components/providers.tsx、frontend/src/components/panels/BasicSettingsView.tsx

## 已知边界

IBM Plex Sans 与思源黑体的 next/font 子集策略不同：IBM Plex Sans 无中文字形（字体本身限制），中文始终回退系统字体；想要中文变化需选"思源黑体"或"霞鹜文楷"。

## 安全回滚

- 备份：_backup/font_fix_20260811/（globals.css、providers.tsx、BasicSettingsView.tsx 共 3 文件）
- 回滚方式：备份覆盖回原路径即可，无数据/后端变更。
- dev 热更新即时生效，无需重启 Electron。

---

# 追加记录：字体切换回归修复（dev 模式 @font-face 缺失 → 字体可用性检测 + CDN 兜底）（2026-08-11，前端 AI）

## 背景

上一条记录的第 2 项改动（删除 Google CDN 注入、改用 next/font 自托管变量）引入回归：用户实测切换字体无效化。

## 根因

- 打包版（out/）：next/font 构建期自托管，IBM Plex Sans @font-face 共 23 条，本地可用。
- dev 模式：next/font 不输出这些 @font-face（dev layout.css 480KB 中 IBM/Noto @font-face 为 0，仅变量定义）；.next 里的 woff2 属旧构建缓存块，dev 页面不加载。
- 旧代码运行时注入 Google CDN 提供 @font-face 所以一直有效；新代码删除该注入后，dev 下切 IBM/思源无字形可用，静默回退 sans-serif，视觉零变化。

## 修复（仅 providers.tsx）

FontProvider 双保险：先应用本地自托管变量，再用 document.fonts.check() 检测字体可用性；缺失时自动回退注入对应 CDN link（恢复旧行为作为兜底），可用时清理残留 CDN link。打包版检测通过纯本地零请求，dev 版自动补 CDN。

## 修改文件

frontend/src/components/providers.tsx

## 验证

- npx tsc --noEmit 通过（exit 0）。
- 手动验证步骤（用户在应用内实测）：设置 → 字体，切换"思源黑体"→ 中文应立即变为思源字形；切回 IBM Plex Sans → 英文/数字字形变化；切"系统默认"恢复。dev 热更新即时生效。

## 安全回滚

- _backup/font_fix_20260811/providers.tsx 为本次字体改动前的原始版本，覆盖回原路径即完整回滚（globals.css 与 BasicSettingsView.tsx 的改动相互独立，不受影响）。

---

# 追加记录：设置面板点击 portal 弹层即关闭的缺陷修复（2026-08-11，前端 AI）

## 现象

用户切换字体时设置面板整个消失，且字体未保存（"没用"）。

## 根因

Panel.tsx 有 document 级 mousedown 点击外部判定：凡点击不在 panelRef 内即 onClose() 关闭整个面板。自定义下拉弹层经 createPortal 挂在 document.body（物理上在面板 DOM 之外），点击选项时：mousedown 先触发 → Panel 判定"点击外部" → 面板卸载 → 弹层随之卸载 → 后续 click 事件丢失 → onSelect 永不触发。面板消失与字体未保存是同一根因的两个表现。同款缺陷同样存在于设置面板的 GroupedModelDropdown（模型下拉）。

## 修复

1. Panel.tsx：handleClickOutside 忽略带 data-portal-popover 标记的弹层内点击（target.closest 判定）。
2. BasicSettingsView.tsx：FontFamilyDropdown 与 GroupedModelDropdown 两处弹层根 div 加 data-portal-popover 标记。
3. 影响面核查：其余 createPortal 用例（ModelSelector/AgentSelector 在聊天页不在 Panel 内；ProjectInitModal 为整屏模态）不受此缺陷影响，未改动。

## 修改文件

frontend/src/components/panels/Panel.tsx、frontend/src/components/panels/BasicSettingsView.tsx

## 验证

- npx tsc --noEmit 通过（exit 0）。
- 用户手动验证步骤（dev 热更新即时生效）：设置 → 字体 → 点开下拉选任一字体：面板保持打开、选项打勾切换、界面字体变化；同样验证模型下拉不再导致面板关闭。

## 安全回滚

- Panel.tsx 备份：_backup/font_fix_20260811/Panel.tsx
- BasicSettingsView.tsx 备份：_backup/font_fix_20260811/BasicSettingsView.tsx（注意：该备份不含用户后续手动加的"未启用"提示与本次 data-portal-popover 标记；如需回滚本条仅删除两处 data-portal-popover 属性并还原 Panel.tsx 判定即可，不必整体覆盖）

---

## 2026-08-11 项目初始化弹窗布局优化（agent 上提 + 空隙压缩 + 工具栏防溢出）

**背景**：弹窗内工具栏 6 胶囊+发送键单行溢出，"标准模式"被发送键压住、箭头裁切；外框与输入组合框之间纵向留白过大；agent 属"创建时绑定不可改"却与可改参数混排。

**改动**（备份：_backup/modal_layout_20260811/）：
1. ProjectInitModal.tsx：AgentSelector 上提至标题行右侧（关闭按钮左边），带 title 提示"会话创建后不可更改"；ChatInput 不再传 allowAgentChange；标题加 ellipsis；头部 padding 20/8→16/4；内容区 padding 8…24→4…12（底部留白砍半）；inputMinHeight 97→64；跳过按钮 padding 6/2→2/0 贴紧卡片。
2. ChatInput.tsx：工具栏左组加 flexWrap:wrap + rowGap:4，窄容器下胶囊换行兜底，发送键永不被压。
3. chat-input/ModelSelector.tsx：触发按钮模型名 span 加 maxWidth:140px + 省略号。

**影响面**：ChatInput/ModelSelector 为共享组件，聊天页同受益（窄窗口不再溢出）；首页 page.tsx 未动（hero 布局无标题行可挂，待确认弹窗效果后再议）。

**验证**：tsc --noEmit 通过。手动验证：新建项目→弹窗应更紧凑；标题行右侧出现助手胶囊且可切换；工具栏不再压键；长模型名显示省略号。

**回滚**：用 _backup/modal_layout_20260811/ 三文件覆盖对应源文件。
---

## 2026-08-11 项目面板交互主次归位（行点击=新开会话 + hover 管理操作组）

**背景**：「新建关联项目」面板里点击已有项目会跳转文件工作区（/projects/{id}/files），与用户预期"在项目里新开会话"相反；且移除关联只能靠侧边栏右键菜单，发现性差。

**改动**（备份：_backup/project_panel_20260811/）：
1. sidebar/ProjectCreateForm.tsx：行主点击改绑 onQuickCreateChat（弹"已关联项目"向导）；行右侧新增 hover 操作组：FolderOpen 图标=打开文件工作区（原行为保留）、Trash2 图标=移除关联，两步确认（点一次变红"确认?"，2 秒自动复位，再点执行）；操作组 hover/确认中才可见，tooltip 注明"会话进回收站，本地文件夹不受影响"。行由 button 改 div role=button（避免嵌套交互元素）。
2. Sidebar.tsx：传入 onQuickCreateChat（先关面板再弹向导）与 onRemoveProject（复用 handleDeleteProject，后端软删+回收站可恢复）。

**语义核实**：后端 delete_project 为软删（is_deleted）+级联软删会话，不碰本地文件夹，回收站可恢复——与"移除关联"语义一致。

**验证**：tsc --noEmit 通过。手动验证：面板点已有项目→弹"已关联项目"向导；hover 行右侧出现两图标；垃圾桶两步确认移除后列表刷新、回收站可恢复；FolderOpen 仍跳文件工作区。

**回滚**：用 _backup/project_panel_20260811/ 两文件覆盖。
---

## 2026-08-11 弹窗二轮紧凑化（agent下拉瘦身只限弹窗 + 头部左贴 + 胶囊压紧）

**改动**（备份：_backup/modal_layout_20260811/ 含 ChatInput 当前版）：
1. chat-input/AgentSelector.tsx：新增 hideDescription prop，隐藏选项介绍行；**仅弹窗传 true**，首页下拉保留介绍不受影响。
2. ProjectInitModal.tsx：头部改 flex-start，agent 胶囊紧跟标题；✕ 用 marginLeft:auto 推最右；AgentSelector 传 hideDescription。
3. chat-input/styles.ts：ghostPillStyle padding 0 10px→0 8px（全局，胶囊视觉空白 28→20px）。
4. ChatInput.tsx：工具栏左组 gap 8→4（全局压紧）；**保留并行改动** projects/onSelectExistingProject props 未触碰。

**验证**：tsc --noEmit 通过。手动验证：弹窗打开 agent 下拉→单行选项、弹层明显变小；胶囊紧跟标题；工具栏更紧凑；首页 agent 下拉仍有介绍行。

**回滚**：_backup/modal_layout_20260811/ 覆盖 AgentSelector.tsx/styles.ts/ProjectInitModal.tsx/ChatInput.tsx。
---

## 2026-08-11 弹窗头部位置回滚（仅位置）

用户未要求移动默认助手胶囊位置，撤销 P2：头部恢复 space-between，胶囊+✕ 回到右侧原位置；移除右组 flex:1 与 ✕ 的 marginLeft:auto。下拉瘦身(hideDescription)与胶囊压紧保留。tsc 通过。
---

## 2026-08-11 重启闪黑 FOUC 修复（首帧主题守卫）

**根因**：tokens.css :root 默认 obsidian(黑) 首帧先画；settings 走 HTTP 拉取（重启/打包冷启动时慢甚至失败且无重试），到达前 resolveVisualTheme(null)=obsidian，到达后才切 studio(白) → 闪黑。

**改动**（备份：_backup/theme_fouc_20260811/）：
1. 新增 lib/theme.ts：主题解析/名单/缓存读写统一模块（resolveVisualTheme、LIGHT_BASED_THEMES、writeThemeCache、applyThemeToDocument）；缓存 key=mfk-visual-theme，value=JSON{t,m}（存解析结果，消费端零逻辑）。
2. providers.tsx：主题定义迁出并重导出保持兼容；ThemeProvider effect 改用 applyThemeToDocument + writeThemeCache（设置到达即写缓存）。
3. store.ts：fetchSettings 成功 / updateSetting / updateSettings 三写点同步缓存（create 加 get）。
4. layout.tsx：next/script beforeInteractive 内联脚本，HTML 解析阶段读缓存立即 setAttribute data-theme + .light/.dark，首帧即上次主题；无缓存静默回默认 obsidian。

**已知边界**：首次安装无缓存仍闪一次黑（产品默认即黑，可接受）；缓存格式改动需同步 layout 内联脚本（两处均有注释互指）。

**验证**：tsc --noEmit 通过。手动验证：切到白主题→完全重启前后端→刷新/重开应直接白、无黑闪；切黑主题重启同理；设置里换主题即时生效且下次启动记住。

**回滚**：删 lib/theme.ts，用备份覆盖 layout.tsx/store.ts/providers.tsx。
---

## 2026-08-11 闪黑守卫 v2（next/script beforeInteractive 在 dev 首帧不可靠）

**复查根因**：v1 用 next/script beforeInteractive，dev 模式下该策略脚本注入首帧 HTML 的时机不可靠（可能由客户端运行时补挂，晚于首帧绘制）→ 缓存已在但脚本跑晚，仍闪黑。

**v2 改动**：layout.tsx 改用原生内联 <script>（无 src，React SSR 原位输出，dev/build/打包一致），置于 body 首位同步执行，早于任何 body 内容绘制。备份目录 layout.tsx 已恢复为 git HEAD 原版（改动前锚点）。

**验证**：tsc 通过。手动验证：刷新/重启应直接上次主题、无闪黑（缓存已于 v1 阶段写入）。
---

## 2026-08-11 黑闪真凶 v3 + 侧边栏收起态持久化

**黑闪真凶**：守卫脚本首帧应用缓存浅色后，ThemeProvider mount effect 在 settings=null 时以 resolveVisualTheme(null)=obsidian 把主题覆盖回黑，等设置到达才变白——守卫被 Provider 自己抵消。修复：effect 加 if (!settings) return;，首帧归守卫、设置到达后 Provider 才接管。

**侧边栏收起态**：AppLayout sidebarCollapsed 此前纯 useState(false) 从未持久化（非回归，是缺失）。修复：key=mfk_sidebar_collapsed，挂载后 useEffect 读回（避 SSR mismatch），toggle 时写入。

**备份**：_backup/sidebar_persist_20260811/AppLayout.tsx、_backup/theme_fouc_20260811/providers.v2.tsx（v3 前中间版）。

**验证**：tsc 通过。手动验证：刷新/重启无黑闪直接白；收起侧边栏→刷新→仍收起；展开→刷新→仍展开。
---

## 2026-08-11 UI 状态持久化缺口修复（全量审计后执行）

**背景**：用户报"聊天页任务栏收起后刷新又展开"。全量审计结论：侧边栏折叠链路本身正确（用户复测确认无问题，此前是修复未生效）；审计发现另外两处同类持久化缺口，本次修复。

**改动 1**：`app/chat/[id]/page.tsx` 项目上下文面板开关 `projectContextOpen`——新增 key=`mfk_project_context_open`，挂载后 useEffect 读回（避 SSR mismatch），setProjectContextOpen 包装为写入版（兼容函数式与直接赋值两种调用点：ChatHeader toggle / Panel onClose）。

**改动 2**：`panels/ModelConfigSection.tsx` ProviderBaseUrlOverride 折叠态 `collapsed`——新增 key=`mfk_baseurl_collapsed`（默认折叠：未写过/null/"1" 均折叠，仅 "0" 展开），点击标题行经 toggleCollapsed 写入。import 补 useEffect。

**未改（有意）**：各弹窗/下拉打开态（刷新归位是正确语义）；BasicSettingsView 实验主题区 expanded（影响极小，待用户决定）。

**备份**：`_backup/ui_state_persist_20260811/`（chat_page.tsx、ModelConfigSection.tsx 改前原版）。注意：PowerShell Copy-Item 对含 `[id]` 的路径需 -LiteralPath，否则通配符静默漏拷。

**验证**：tsc --noEmit 零错误。手动验证：① 聊天页打开项目上下文面板→刷新→仍打开；关闭→刷新→仍关闭。② 设置→模型→高级区展开 Base URL 配置→刷新设置面板→仍展开；折叠→刷新→仍折叠。

**回滚**：用备份目录两个文件覆盖原位即可，无其它联动。
---

## 2026-08-11 任务执行卡片折叠态按会话持久化（“任务栏”真目标）

**背景**：用户所报“任务栏收起后刷新又展开”真目标 = 聊天页输入框上方的“任务执行”卡片（TaskProgressCard），非侧边栏/项目上下文面板。此前我误判目标，先改了两处审计缺口（用户确认保留）；本条才是任务栏本体修复。

**根因**：TaskProgressCard `collapsed` 纯 useState(false)，刷新必重置为展开。

**改动**：
1. `TaskProgressCard.tsx` 新增可选 prop `chatId`；折叠态按会话持久化 key=`mfk_task_card_collapsed_${chatId}`：挂载/切换会话读回（同时同步任务计数基线，防误触发自动展开）；手动折叠/展开写入；保留“新任务到达自动展开”智能行为并同步写 "0"；chatId 缺失则不持久化（行为同改前）。
2. `app/chat/[id]/page.tsx` 调用点传 `chatId={chatId}`。

**用户决策**：已改两处保留；任务栏按会话记忆（非全局）。

**备份**：`_backup/ui_state_persist_20260811/TaskProgressCard.tsx`（改前原版）。

**验证**：tsc --noEmit 零错误。手动验证：会话 A 收起任务卡→刷新→仍收起；切到会话 B→默认展开（各自独立）；会话 A 发新任务→自动展开（智能行为保留）。

**回滚**：备份 TaskProgressCard.tsx 覆盖 + page.tsx 去掉 chatId 传参。
---

## 2026-08-11 “修改不生效”排查报告（只查不改）

**用户反馈**：任务栏修复无效，且“最近经常修改代码但没反应”。

**进程取证**：Electron 窗口 15:57 启动（bat 启动、ELECTRON_DEV=true → dev 路径加载 localhost:3000）；3000 dev server 16:58 重启过；TaskProgressCard 改动 17:26 落盘；out/ 打包产物停在 8/10 23:24。

**三大机制**：① HMR（Fast Refresh）保留组件 state、不重跑 deps 未变的挂载 effect——“挂载读持久化”类改动热更新后当前窗口症状不变，必须 Ctrl+R 全量刷新；② 窗口与 dev server 不同代（15:57 vs 16:58），HMR 连接可能静默失效；③ 打包版陷阱：out/ 是昨日构建，若用安装版快捷方式启动则任何代码改动都不生效（需 npm run build）。

**待用户反馈**：Ctrl+R 全量刷新后复测任务栏；若仍无效则完全重启 start-desktop.bat 再测。两步结果决定下一步定位方向。
---

## 2026-08-11 任务栏失效真凶 v2 + 上轮诊断纠正

**纠正**：上轮“HMR/运行时陈旧”诊断错误（用户完全重启后仍无效，证伪）。真凶在代码内部。

**取证手段**：读 Electron localStorage 落盘文件（`%APPDATA%\mfkagent\Local Storage\leveldb\000006.log`，静态文件不违禁）——发现 `mfk_task_card_collapsed_138` 先写 "1"（用户收起成功）后被覆写 "0" 两次。

**真凶**：TaskProgressCard 的“新任务自动展开” effect 无 live 守卫——刷新后 tasks 从缓存/历史恢复导致 length 增长，被误判为“新任务到达”，setCollapsed(false) + 覆写 "0"，把用户收起态干掉了。

**修复**：新增 `live` prop（page.tsx 传 isSending）；自动展开+写 "0" 仅限 live=true；刷新/恢复路径只同步计数基线，绝不覆写。

**验证**：tsc 零错误。手动验证：收起→重启脚本→刷新→仍收起；流式发新任务→仍会自动展开。

**教训**：① “写入成功但读回无效”时，先查有没有第三方写入覆写（LevelDB log 是合法取证源）；② 自动行为（自动展开）与持久化用户意图共存时，自动行为必须带“实时性”守卫。
---

## 2026-08-11 侧边栏项目区视觉三级梯度调整

**背景**：用户报“项目工作区这一行太高”+“副级跟子级一样高”+“绘图图标比项目图标大”。三处全部坐实：
- L1 标题头 28-30px ≈ L2 项目行 32px ≈ L3 会话行 32px，无层级梯度
- L1 标题头文本 12px / 图标 13px / + 按钮 22×22 偏高
- ThinkingOrb 20 > 文件夹 14（“绘图比项目大”的罪魁）

**改动（三级梯度 22/30/27 px）**：
1. `Sidebar.tsx` 两处标题头：padding 6px→2px、文本 12→11、chevron/主体图标 13→12（复用 `--sidebar-icon-size-sm`）、右侧 + 按钮 22×22→20×20、+ 内图标 14→12。**通用对话头是 sticky 吸顶，原 top: 36 同步改 top: 28** 保留“紧贴项目头下方”语义。
2. `ProjectNode.tsx`：项目行 padding 5px→4px；More 内图标 14→12（次级动作统一小档）。
3. `ChatRow.tsx`：会话行 padding 5px→3px；More 内图标 14→12。

**未改/受抑**：ThinkingOrb 未压到 14——`thinking-orbs` 库 `OrbSize = 64 | 20`（字面量类型），最小只能 20，14 会 TS2322。考虑过 transform: scale(0.7) 缩放（画布分辨率 20、显示 14），会路微失真且是 hack，故不采纳。需跟用户确认：(a) 接受 20 vs 14 的现状（库限制）；(b) 把项目文件夹改成 16 以缩小差距；(c) 换自绘。为不返工，**采默认 (a)交付**，用户提后腰改。

**备份**：`_backup/sidebar_visual_20260811/`（Sidebar.tsx / ProjectNode.tsx / ChatRow.tsx 改前原版）。

**验证**：tsc --noEmit 零错误。手动验证：侧边栏三级高度明显拉开（22/30/27）；通用对话头滚动时仍吸顶在项目头下。